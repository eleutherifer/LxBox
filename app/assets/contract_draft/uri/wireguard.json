{
  "_overlay": true,
  "_why": "Секция `mappers.uri` приехала реестром исполняемой (контракт 1.1.23+) и ведёт ОБЕ формы ссылки — `url` (key@host:port) и `conf_b64` (целый wg-quick под base64). Здесь остаются только те ключи, которые у нас обязаны быть иными; у каждого своё `_why`. Полной копии секции тут нет и быть не должно: она стала бы вторым источником правды и протухла бы на первом же синке.",
  "mappers": {
    "uri": {
      "emit": {
        "_why": "Реестр 1.1.46 объявил `emit` (form/param_order совпали и сняты). Остаются `omit_default` и `form_from`: у нас AWG-узел с awg-полями уезжает `wireguard://` через `any_set` (снимок emit_before480), у реестра `form_from` по `$kind`.",
        "form_from": {
          "any_set": {
            "wireguard": [
              "jc",
              "jmin",
              "jmax",
              "s1",
              "s2",
              "s3",
              "s4",
              "h1",
              "h2",
              "h3",
              "h4",
              "i1",
              "i2",
              "i3",
              "i4",
              "i5",
              "header_protection_key",
              "content_padding_addition",
              "rekey_after_time",
              "rekey_timeout",
              "reject_after_time",
              "keepalive_timeout",
              "max_handshake_attempts",
              "random_trailers",
              "disable_cookies"
            ],
            "*": "wireguard"
          }
        },
        "$impl": "НАПИСАНИЕ СХЕМЫ — решение владельца, согласовано с лаунчером: AWG-узел С awg-полями уезжает `wireguard://…?jc=…`, то есть ровно тем написанием, каким уезжал всегда (судья — `emit_before480.json`: все 65 кейсов, включая 24 awg-именованных, несут `wireguard://`). Написание `awg://` остаётся ТОЛЬКО за узлом «род awg, ни одного awg-поля в теле», и выразить его здесь сегодня нечем: рода в каноническом теле нет, а вход эмита — тело. Обе ветки `any_set` дают одно написание намеренно — так перечень awg-путей остаётся на виду и не выглядит забытым; когда роду найдётся место в модели, ветка `awg` получит своё написание без правки грамматики.",
        "_why_omit_default_removed": "Синк 1.1.53 (§49 п.13) — `omit_default: [\"server_port\"]` СНЯТ: дефолта порта в секции wireguard нет, и запись ничего не выражала. Опускать порт «по умолчанию» там, где умолчания не объявлено, значит опускать его всегда — а этого ни одна сторона не просила.",
        "_delta_id": "D533-wg-awg-scheme-spelling"
      }
    }
  }
}
