{
  "_draft": "ОТСТУПЛЕНИЕ LxBox от секции реестра (с контракта 1.1.22, 4aeca7a7). Файл НЕ дублирует секцию: загрузчик накладывает эти ключи поверх реестровых.",
  "_overlay": true,
  "mappers": {
    "uri": {
      "_why_include_dropped": "D133-26 (контракт 1.1.26): реестр сам подключил `dialer#uri` к этой секции — принял нашу сторону. Оверлей include снят, список ведёт реестр. Дельта закрыта, оверлей остаётся только ради `emit`.",
      "emit": {
        "omit_default": [
          "fp"
        ],
        "_why_omit_default": "§480 W8 — `security` ВЫЧЕРКНУТ из omit_default и объявлен emit_when: always. У Xray-клиентов (v2rayN/v2rayNG/NekoBox/Hiddify/Shadowrocket) умолчание security у vless — `none`: ссылка БЕЗ параметра читается ими как узел БЕЗ TLS. Опустить его значило бы отдать наружу ссылку, которая у соседа поднимется открытым соединением. `fp` остаётся в omit_default: у vless `fp=random` не писался и раньше."
      }
    }
  }
}
