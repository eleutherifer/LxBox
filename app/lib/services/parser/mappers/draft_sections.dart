/// §480 W1 — СОСТАВ черновика секций-мапперов.
///
/// Список лежит ЗДЕСЬ, а не в пакете движка, по той же причине, по которой
/// движок не знает ни одного имени схемы: имена файлов протоколов — это имена
/// схем, и греп-страж (`engine_no_scheme_names_test.dart`) их там не терпит.
/// Загрузчику список передаётся аргументом.
///
/// Перечислять приходится явно: ассеты Flutter в рантайме не перечисляются, и
/// «угадать» имя файла — значит молча потерять секцию.
///
/// Черновик ВРЕМЕННЫЙ. Каждая строка уходит отсюда, как только секция того же
/// протокола приедет исполняемой в реестре контракта: загрузчик берёт реестр
/// первым, и лишний файл черновика просто перестаёт читаться.
library;

/// Файлы `assets/contract_draft/uri/<имя>.json` без расширения.
///
/// Перечислены только те, что движок СЕГОДНЯ исполняет: секция схемы,
/// переехавшей на движок, плюс общие блоки, которые она подключает через
/// `include`. Прочие черновики (`vmess`, `socks`, `wireguard`, каталоги
/// `xray/` и `conf/`) волнами W2–W5 написаны вперёд движка и лежат рядом; они
/// добавятся сюда вместе со своей волной — раньше грузить их незачем, а
/// молчаливо загруженная, но никем не исполняемая секция создаёт ложное
/// впечатление, будто схема уже переехала.
/// Контракт 1.1.15 привёз секции `trojan` и `vless` исполняемыми, вместе с
/// общими блоками `tls`/`transports`/`dialer` по диалектам: загрузчик берёт
/// их ИЗ РЕЕСТРА, и временные ПОЛНЫЕ копии этих файлов удалены.
///
/// Оставшиеся одноимённые файлы — ОВЕРЛЕИ (`_overlay: true`): не копия
/// секции, а только те ключи, которые у нас обязаны быть иными. Каждый такой
/// ключ — красный кейс, переданный лаунчеру: дельты на него в его
/// `DELTAS.md` нет, а тело РАБОЧЕГО узла меняется. Оверлей снимается вместе
/// с ответом лаунчера, а не молча.
const kDraftFiles = <String>[
  // Секции протоколов, вид источника `uri`. Имя без каталога загрузчик
  // читает как `uri/<имя>` — так короче писался список, пока вид источника
  // был один.
  // `trojan` и `vless` СНЯТЫ синком 1.1.53 (дельта D133-E10, §49 п.16):
  // у обоих оставался один ключ `emit.omit_default`, которым они заставляли
  // писать `security`. Теперь это НОРМА РЕЕСТРА — у trojan через
  // `emit_when: {security: always}`, у vless снятием исключения
  // `omit_default: ["security:reality"]`: у Xray-клиентов отсутствие
  // `security` означает ровно `none`, и reality-узел без параметра
  // прочтётся БЕЗ TLS. Прав оказался LxBox, оверлеи стали излишни.
  // §480 W4 — волна ссылочных схем LxBox. Секций, которых в реестре нет
  // вовсе, черновик держит ЦЕЛИКОМ; файл называется по ТИПУ ТЕЛА, как
  // адресует загрузчик.
  //
  // Контракт 1.1.22 забрал в реестр tuic, hysteria2 и masque. Полные копии
  // сняты по тому же правилу, что у socks/ssh/shadowsocks ниже: загрузчик при
  // исполняемой секции реестра берёт реестр, и копия молча становилась вторым
  // источником правды. У tuic и hysteria2 файл остался ОВЕРЛЕЕМ на `emit` —
  // обратного хода реестр для них ещё не объявил; у masque своего `emit` не
  // было, и файл снят целиком.
  //
  // Синк 1.1.37: `tuic` снят ЦЕЛИКОМ. Реестр привёз и `emit` этой схемы
  // (вместе с нашим `names.insecure = allow_insecure`, принятым как есть), и
  // правило пустого пароля тем же примитивом `on_empty` под тем же именем
  // кода `password_empty` — отступления не осталось ни одного.
  //
  // `hysteria2` из списка СНЯТ синком 1.1.53: файла у него нет с того же
  // синка 1.1.37, и имя висело в перечне мёртвым — загрузчик искал
  // `uri/hysteria2.json`, не находил и шёл дальше молча.
  'wireguard',
  // §480 — секция приехала реестром, черновик остался ОВЕРЛЕЕМ на те записи,
  // которые у нас обязаны вести себя иначе. Полные копии (socks, ssh,
  // shadowsocks) удалены: загрузчик при исполняемой секции реестра брал
  // реестр и копию игнорировал молча, то есть копия была вторым источником
  // правды, протухавшим незаметно. Страж — в `mapper_sections_w4_test`.
  'anytls',
  // `http` СНЯТ синком 1.1.53 (§49 п.1): компенсирующая запись
  // `$security_none_https` больше не нужна. Лаунчер закрыл ту же дыру
  // РАЗДЕЛЕНИЕМ блока (`tls#uri_security`, его http подключает), а у нас
  // заработало правило §7.1 — условная запись `security` схемы (гейт
  // `when.scheme.not_in: [proxy-https]`) блочную больше не изымает, и ветка
  // `none` блока доезжает до https-написания штатно.
  // `naive` СНЯТ синком 1.1.53 (§49 п.12): в файле остался один
  // `emit.omit_port: 443`, а реестр порт ПИШЕТ (решение владельца «всё по
  // рекомендации»), и исполняемых ключей не осталось.
  // `shadowsocks` СНЯТ синком 1.1.53 (дельта D133-E11, §49 п.17): реестр
  // объявил `userinfo.padding` ЛОЖЬЮ — эталон SIP002 пишет userinfo
  // base64url без хвостовых `=`, и вид ссылки принадлежит ФОРМАТУ СХЕМЫ.
  // Прав оказался LxBox, и оверлей стал побайтно равен реестру. Чтение обеих
  // форм остаётся у обеих сторон (`userinfo.decode: base64?`,
  // percent-декод идёт ДО base64, поэтому доезжает и `%3D`).
  // §480 — секция сама приехала реестром (1.1.11); черновик остался
  // ОВЕРЛЕЕМ на две записи транспортного селектора. Загрузчик берёт реестр и
  // накладывает их сверху.
  'vmess',
  // Общие блоки (`include`): их записи вмонтируются в секцию схемы. Блок
  // ОДИН на все диалекты (`blocks.uri`, `blocks.xray`), поэтому лежит в
  // каталоге того вида источника, с которого начался.
  'tls',
  'transports',
  // `transports` СНЯТ синком 1.1.53 (§49 п.3, 5, 7, 8): реестр забрал все
  // шесть отступлений — три `emit_as: raw` у xhttp (норма PRIMITIVES
  // §0.12a: булев без `emit_as` пишется СЛОВОМ), плоские `ws.ed`/`ws.eh`
  // (прав корпус: кейсы ждут `json_field_unknown`), `ws.host` (оверлей
  // читал только `headers.Host` и терял плоский `wsSettings.host`) и
  // `xhttp sessionPlacement`/`sessionKey` (реестр читает плоскую форму с
  // 1.1.50, оверлей был старше реестра).
  // `dialer` СНЯТ синком 1.1.53 (§49 п.6): отрицательный `interval` БЕЗ
  // положительного `idle` теперь читает реестр, а пара `idle: 30 +
  // interval: -5` даёт `tcp_keep_alive: 30s` БЕЗ флага
  // `disable_tcp_keep_alive` — наш флаг на первой паре был ошибкой
  // (кейсы `body/xray/sockopt_keepalive_negative_interval*`).
  // §480 W5 — вид источника `xray`: карта Xray-outbound'а. Здесь каталог
  // обязателен: у одного протокола черновиков столько же, сколько видов
  // источника, и в одно пространство имён они не кладутся.
  // `xray/trojan` и `xray/vless` СНЯТЫ синком 1.1.53: их `forms` убрала
  // задача 532 (со строгим `type_of` оверлей отнимал plain-TCP узел), и
  // после этого в файлах не осталось ни одного ИСПОЛНЯЕМОГО ключа — одна
  // проза о закрытых расхождениях. Пустой оверлей загрузчику работы не
  // даёт, а читателя вводит в заблуждение, будто отступление живо.
  // `xray/shadowsocks` СНЯТ синком 1.1.53 (§49 п.14): оставался один
  // `empty: significant` у пароля. У реестра запись `required: true`, и это
  // РУБЕЖ против плейсхолдеров просроченной подписки — сам Xray отбраковывает
  // узел при пустом пароле (`xray_protocols.go:284-286`). Пустой пароль у
  // ss-узла означает не «законное значение», а «сервер вернул заглушку».
  // `xray/socks` СНЯТ синком 1.1.52 (§514): волна 1.1.50 привезла
  // `mappers.xray` у socks ИСПОЛНЯЕМОЙ секцией реестра, и загрузчик при такой
  // секции берёт реестр — полная копия черновика молча становилась вторым
  // источником правды, протухающим незаметно (то же правило, по которому
  // сняты socks/ssh/shadowsocks вида `uri`). Реестровая секция ШИРЕ нашей:
  // у неё есть `label` с фолбэком и `$extra_users`, а `version: "5"` она
  // ставит `defaults` — наш черновик версию не писал вовсе. Расхождение по
  // §321 (socks самостоятельным узлом подписки не становится) живёт НЕ в
  // секции, а в отборе `_xrayToSpec`, и снятием файла не затронуто.
  // `xray/hysteria2` СНЯТ синком 1.1.28: контракт привёз ДВЕ секции
  // `mappers.xray` со взаимоисключающими `detect` по версии, и обе
  // исполняемы. Наш черновик ронял версию 1 кодом `protocol_unsupported`;
  // у реестра она становится живым узлом другого рода. Дельта принята
  // (TASKS_LXBOX §26), полная копия при исполняемой секции реестра молча
  // проигрывала бы ей и перестала исполняться.
  // §480 W5 — вид источника `singbox`. Все 13 секций СНЯТЫ синком 1.1.53
  // (§49 п.9): контракт забрал их в реестр (`mappers.singbox` у каждой
  // схемы), и при исполняемой секции реестра загрузчик берёт реестр —
  // копия молча становилась вторым источником правды, протухающим
  // незаметно. Взяты они с одной правкой: у форм `endpoint`
  // (`wireguard`, `tailscale`) предикат назывался `has_key`, которого в
  // грамматике нет, и заменён на `required_keys`; наш движок читает оба
  // написания (`interpreter.dart`), поэтому форма срабатывала и до
  // правки.
  // §480 W6 — реестр ВИДОВ ИСТОЧНИКА жил здесь черновиком. Контракт 1.1.41
  // завёл его нормой (`registry/source_kinds.json`), черновик снят: состав
  // веток совпал до записи, расхождения были только в написании имён
  // (`source_kinds.kinds`/`source_kind` против `sources`/`kind`), и их
  // читает загрузчик.
];
