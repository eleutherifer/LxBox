# §506 — Обзор изменений

| | |
|---|---|
| **Статус** | **Released в v2.25.2** (24.09.2026). Готово (отчёт; код не правился). Ноты и архив v2.25.0 приведены к пререлизу; приёмка на эмуляторе — владелец 20.09.2026. Тег не ставился. |
| **Дата** | 2026-09-20 |
| **Источник** | сверка `develop` с тегом v2.24.4 перед v2.25.0 |
| **Связанные** | фичи [460](../features/460%20contract-registry-bundle/spec.md), [472](../features/472%20unified-parse-pipeline/spec.md), [478](../features/478%20core-rejected-node-auto-disable/spec.md), [480](../features/480%20registry-driven-mapper/spec.md); задачи 479, 489–505; §291 |
| **База** | тег `v2.24.4` (`1961b20a`, 2026-09-17 23:24) |
| **Голова на момент сверки** | `develop` `6d3fe599` (`v2.24.4-dev.267`) |

Журнал рабочего цикла: верхнеуровневый состав с последнего релиза и
архитектурный разбор. Дефекты **задокументированы, не чинились**. Тег
v2.25.0 не ставился.

## Docs to update

- эта задача (отчёт): D1 — вердикт владельца 20.09.2026 (не чинить);
  D17 — недоработка кеша `body` / `warnings` (владелец, 20.09.2026)
- [478](../features/478%20core-rejected-node-auto-disable/spec.md) — норма
  «автомат = кнопка Start», Kotlin/прочий Dart не оборачивать

---

## 1. Сводка

С 17 по 20 сентября в `develop` ушло **267 коммитов**, **501 файл**,
**+107 758 / −9 440** строк. Это не набор заплат, а смена основания разбора:
рукописные парсеры заменены движком от общего с лаунчером реестра, поверх
него — страховка от отказа ядра и единый (заявленный) показ уведомлений.

| Было в v2.24.4 | Стало на голове |
|---|---|
| Контракт 1.1.0-эпохи синка 16.09 (`b90ff80d…`) | Зеркало **1.1.46** (лаунчер `7e2945bf`) |
| Ядро `v1.14.1-lx.4` | Ядро **`v1.14.1-lx.8`** |
| 13 рукописных URI-парсеров + отдельные Xray/INI/emit | Движок `parser/engine/` без имён схем |
| Один негодный узел валил весь VPN (#147) | Страховка выключает названный узел и поднимает остальных |
| Предупреждение — одна строка под узлом | Уровни ✖/⚠/ⓘ, карточки, Diagnostics |

Главный акцент нотсов (`docs/releases/v2.25.0.md`) совпадает с
кодом: **сближение с singbox-launcher 2.0.0**. Второе — страховка #147.

Релизный гейт **по нотам закрывается этим коммитом**: `RELEASE_NOTES.md` /
`docs/releases/v2.25.0.md`, CHANGELOG `## [2.25.0]`. Smoke на эмуляторе принят
владельцем 20.09.2026. Телефон и команда на тег — за владельцем. Pre-flight от
19.09 (`5ca96a71`) был зелёный; checks на голове после §505 — зелёные.

Снятие Boosty из donate/support/ссылок принято владельцем 20.09.2026 и входит
в состав v2.25.0.

---

## 2. Что сделано, верхний уровень

### 2.1. Реестр и единый конвейер разбора (фичи 460 → 472 → 480)

Это ядро релиза. Три слоя, один за другим:

1. **460** — реестр контракта едет в APK (`assets/contract/`). Санитайзер
   судит тело по схеме; тексты предупреждений — из `warnings.json`;
   «Learn more» ведёт в зеркало `docs/contract/`, не на main лаунчера.
2. **472** — один конвейер: любой вход → сырая карта → санитайзер →
   `parseSingboxEntry` → `NodeSpec`. Шаги 1–9 закрыты; identity не сдвигали.
3. **480** — первый слой конвейера тоже от реестра: маппер ссылок / Xray /
   INI и сборка ссылки исполняют таблицы. Рукописные `*_mapper.dart` сняты;
   `uri_parsers/*.dart` остались **именами** (`parseVless` →
   `parseUriViaPipeline`). Страж `engine_no_scheme_names_test` запрещает имена
   схем в движке.

Синки контракта шли плотно: 1.1.0 → … → **1.1.46**. Снято 25 оверлеев
класса A. В `contract_draft/` остаётся **28+ файлов** расхождений B/C
(ss padding, xhttp `emit_as`, ws.eh, vmess `json_map`, xray forms,
singbox-секции нашего вклада).

Сопутствующие задачи: 463–476, 481–488, 491–493, 495. Видимые починки для
человека — в черновике нотсов: Xray все четыре формы, base64 из буфера,
`splithttp`→`xhttp`, hysteria2 `mport`, REALITY `pbk`, `naive+quic`, VMess
gRPC, TUIC пустой пароль, Xray без порта, WG без Endpoint,
`tls.enabled:false`.

Ядро: lx.5 (REALITY short_id — ошибка, не паника), lx.7 (тип+тег в ошибке
инициализации — страховка на этом стоит), lx.8 (gRPC `service_name` как есть).

### 2.2. Страховка от отказа ядра (фича 478, #147)

Автомат `CoreRejectGuard`: сигнальный старт → разбор строки ядра → выключить
названный узел тем же выключателем → тихий `checkConfig` до 10 кругов →
вопрос → финальный старт. Вердикт **не** едет в бэкап (§489). Плашка на
главном, лист «N servers disabled», навигация в Diagnostics
(§498/§501/§503). Ревью закрыто §490.

Слой автомата чистый: `services/core_reject/` не знает виджетов. Хост —
фасад над контроллерами. Это соответствует §291.

### 2.3. Уведомления и значки (471, 479, 496–505)

Уровни как у лаунчера; шторка Why / What to do; вкладка Notifications сначала
отдельная (§497), затем **слита в Diagnostics снизу** (§501) — активная
диагностика важнее. Значки: Servers (§499), главный экран (§502), холодный
старт / одиночный сервер (§505, два файла спеки). Probe-бар без
«Test servers» (§496). Причина отказа ввода листом (§500). Подсветка новой
записи на Servers (§504; виджет-тест не влит).

### 2.4. Прочее

- Debug API: долги после прогона страховки (§494).
- `tool/worktree_bootstrap.sh` (§487), реестровые тесты на зеркале
  `assets/contract` (§486).
- Черновики fastlane `22500501/502` для v2.25.0.
- Задача **477** (грамматика vless encryption) снята в пользу 478: ядро
  отказывает — страховка выключает.

---

## 3. Архитектура: что держится

- **Конвейер разбора** описан в `docs/ARCHITECTURE.md` (движок / санитайзер /
  emit от одной таблицы). Заголовок файла всё ещё говорит «parser v2 /
  spec 026» — устарел, тело уже про 480.
- **Страховка** вынесена из экрана в `runCoreRejectGuard` — Debug API зовёт
  ту же связку. Вердикт режется на экспорте (`core_reject_backup.dart`),
  тесты это держат.
- **Фасады 478** не тащат `DebugContext` в домен. `CoreRejectState` —
  отдельный синглтон прогона (как CrashBanner), не поле HomeState.
- **Эталоны §291** (channels / subs / HomeController) новыми фичами не
  ломались. DNS `services → screens` — старый долг §300, не этого цикла.
- Identity конвейера охраняется снимком `before_480_identity_snapshot`;
  дельты — явным списком, не молча.

---

## 4. Найденные дефекты

Не чинил. Нумерация сквозная. «Долг спеки» = уже записано авторами;
«новое» = всплыло при этой сверке.

### D1. Страховка не является единственным входом старта — и на Dart тоже

**Серьёзность:** высокая как находка; **не чинить**  
**Тип:** новое (спека 478 признавала только Kotlin-обход)  
**Вердикт:** 20.09.2026, владелец. Норма записана в спеку 478.

Спека 478 говорит: страховку зовут кнопка Start и
`start-vpn-headless?guard=true`; остальное (QS, сторож, Intent, автозапуск)
идёт мимо и падает как прежде. Это подтверждено:

| Вход | Путь | Страховка |
|---|---|---|
| Кнопка Start на главном | `home_screen.dart:960` → `runCoreRejectGuard` | да |
| Debug `start-vpn-headless?guard=true` | `action.dart:298` | да |
| Debug / Automation `start-vpn` | `guard=false` → `home.start()` | нет (осознанно, §494) |
| QS-тайл | `LxBoxTileService.kt:99` → `BoxVpnService.start` | нет |
| Boot / auto-start | `BootReceiver.kt:275` | нет |
| Сторож | `VpnWatchdog.kt:125` | нет |
| Intent API §047 | `LxBoxIntentReceiver.kt:90`, `LocaleSettingReceiver.kt:48` | нет |

**Dart-входы без страховки, которых спека на сверке не называла:**

1. **Кнопка Reload, VPN выключен** («Rebuild config + connect») —
   `home_controls.dart:318` → `_rebuildAndStart` → `_controller.start()`
   (`home_screen.dart:892`).
2. **Reload при живом туннеле** — `_rebuildAndReconnect` → `reconnect()` →
   `_startInternal()` (`home_controller.dart:950`).
3. **Автоподключение после смены workspace** — `home_screen.dart:475` →
   `_controller.start()`.
4. **In-place reload / автоприменение §338** при живом туннеле —
   `reloadVpn()` → `startOrReloadService`.

Автомат висит не на `start()`. `HomeController.start()` / `_startInternal()` —
примитив «скажи native поднять сервис». Прогон (`runCoreRejectGuard`) —
обёртка жеста кнопки Start: она сама вызывает `startAndAwaitVerdict()` →
`start()`. Reload, workspace и меню Reconnect бьют в примитив напрямую —
поэтому автомата не видят. Класть проверку внутрь `start()` нельзя: автомат
его вызывает, будет рекурсия.

**Почему не оборачивать всё.** Автомат имеет право выключать узлы, потому
что человек нажал Start. Размазать его на любой подъём VPN — тихое
выключение без этого жеста (как раз то, чего спека 478 не хотела на
путях без UI).

**Kotlin (плитка QS / boot / сторож / Intent) — не чинить.** Это не сборка
нового состава. Все четыре зовут `BoxVpnService.start` → `ConfigManager.load()`
→ последний JSON, который Dart уже записал. Типичный путь: Start со
страховкой выключил негодное, пересобрал, записал, поднял; перезагрузка /
плитка / сторож поднимают этот файл. Сторож ещё чище: возвращает то, что
уже крутилось. На диске лежит последний *сохранённый* конфиг, не обязательно
последний удачно стартовавший (Dart мог записать «Rebuild only» без старта) —
это не повод чинить Kotlin: сборку по-прежнему делает Dart.

Что это за входы:

- **QS** — плитка L×Box в шторке быстрых настроек. Тап включает/выключает
  VPN, приложение не открывается (экран нужен только пока нет согласия VPN).
- **Boot** — галка автозапуска: после загрузки телефона `BOOT_COMPLETED` →
  тот же старт с диска.
- **Сторож (§428)** — не кнопка. Пока туннель «желателен», живой сервис
  переставляет будильник; процесс умер без явного Stop — будильник поднимает
  сервис снова. Ручной Stop не перебивает.
- **Intent (§047)** — Tasker / MacroDroid / `am broadcast`. По умолчанию
  выкл. Команда start-vpn идёт в native напрямую, не в кнопку Start.

**Прочий Dart — тоже не чинить.** Если с диска стартует тот же конфиг, что
уже держал туннель, — взлетит. Если конфиг новый (Reload dirty, reconnect
после пересборки, смена слота, автоприменение) — это «примени другое», а не
«верни как было». Автомат здесь не должен сам выключать узлы: отказ, туннель
лежит, человек жмёт Start — проверка с согласием. Снятие живого туннеля при
Reload/Reconnect — цена смены состава, не баг 478.

Полный фасад «безопасный start/reconnect везде» не делаем. `reloadVpn()` в
эту команду и не входил бы: это перечитывание файла в живом box, не
stop+start.

Нотсы: не обещать «на любом способе поднять туннель». Правда — «кнопка Start
выключает негодный узел и поднимает остальных».

### D2. Три источника уведомлений, не один

**Серьёзность:** высокая  
**Тип:** частично закрыто §505, шов остался

Заявленный единый API — `warningsForConfigTag` (разбор + вердикт + сборка).
Фактически:

| Поверхность | Откуда берёт |
|---|---|
| Главный экран (presenter) | `warningsForConfigTag` + `lastBuildWarningsByTag` |
| Diagnostics одиночного / члена папки | то же |
| Подпись одиночного на Servers | то же |
| Список узлов **подписки** | только `node.warnings` (`subscription_node_list.dart:76`, `node_inspect_screen.dart:89`) |
| Счётчик на строке подписки/папки | `entryWarningSummary` — разбор + вердикт, **без** сборки (`entry_warnings.dart:143–154`) |
| Inspect из листа страховки | вердикт дописывается мутацией `stampNodeWarnings` перед пушем (`core_reject_ui.dart:102`) |

`lastBuildWarningsByTag` живёт **только в памяти сессии**
(`subscription_controller.dart:145–148`), заполняется в `generateConfig`.
Холодный старт без dirty/пустого конфига **не** пересобирает
(`home_screen.dart:458`) — гард реестра на сборке на главном снова пуст,
пока кто-то не нажмёт Reload. §505 починил разбор+вердикт; предупреждения
**сборки** после рестарта по-прежнему дырявые.

Отсюда же дефект, который владелец поймал на эмуляторе (AWG MTU на Servers
есть, на главном нет): два агента написали **две спеки с номером 505**
(`505-home-badge-cold-start.md` и `505-home-node-badge-user-server.md`) с
разным «единым источником». Задачей §513 первая слита во вторую и удалена. Код ближе ко второй, но подписка и счётчик
строки её не используют.

Корень дырявой сборки после рестарта — D17: карта в памяти вместо полей
записи `body` / `warnings`.

### D3. Доменная склейка уведомлений лежит в `screens/`

**Серьёзность:** средняя  
**Тип:** новое

`warningsForConfigTag` / `mergedNodeWarnings` / `entryWarningSummary` живут
в `screens/subscriptions_screen/entry_warnings.dart`. Их импортируют главный
экран, настройки узла, папка, подписка. Это не презентация — это правило
«какие отметки принадлежат тегу». По §291 такое место — фасад домена
(`services/` или контроллер), не экран источника.

`mergedNodeWarnings` уже в `controllers/…/core_reject_ops.dart`; UI-склейка
с сборкой — рядом, в screens. Два модуля, одна ответственность.

### D4. Вердикт дописывается мутацией `NodeSpec.warnings`

**Серьёзность:** средняя  
**Тип:** новое (следствие 478 + 283)

`stampStoredVerdicts` / `stampNodeWarnings` **вставляют** `core_rejected` в
список предупреждений разобранного узла (`core_reject_ops.dart:559–579`).
Комментарий сам говорит: предупреждения при пересчёте замещаются целиком,
вердикт иначе пропадает. Отсюда:

- часть экранов читает `node.warnings` и «случайно» видит вердикт, если
  штамп уже прошёл;
- часть читает `mergedNodeWarnings` без мутации;
- `unstampCoreRejected` обязан пройтись по инстансу при ручном включении.

Это тот же класс ловушки, что §283 (кэш identity / подмена инстансов).
Штамп на объекте — скрытый второй канал рядом с `nodeWarnings` в хранилище.

### D5. Реестр объявляет примитивы, движок их не исполняет — молча

**Серьёзность:** средняя  
**Тип:** долг спеки 480, проверено

- `emit.refuse_when` (WG, больше одного пира) и `round_trip_only: "emit"`
  (dialer→detour) — в реестре 1.1.42, движок не читает. Линтер не краснеет.
  Сейчас недостижимы из-за сводки `.conf` к одному пиру **до** эмита; когда
  многопировый узел доедет до эмита целиком, отказ не сработает, и это
  узнает пользователь, не CI.
- `on_unrecognized` (вместо ветки `clash_yaml`) не исполняется. Запасной
  путь в `body_decoder.dart:96–100, 206–211` рукописный: JSON без своей
  ветки не должен превратиться в «список ссылок из одной строки». Две дороги
  опознания документа.

### D6. Оверлеи и красный корпус — две правды emit

**Серьёзность:** средняя  
**Тип:** долг, проверено

`contract_draft/` держит расхождения, без которых Copy link на телефоне и на
десктопе даст разную ссылку: ss padding (`=` срезается у нас), xhttp
`emit_as`, ws `eh`, vmess `json_map`. Это не баг сборки — осознанный раскол
сторон. Для пользователя «скопировал на телефоне → вставил на десктопе»
round-trip **не** гарантирован на этих схемах, хотя нотсы говорят обратное
общим тезисом.

Красные кейсы корпуса (спека 480 §12/§15): `naive/empty_host_rejected`
(`parse_error` vs `emit_error`+`field_missing`); пачка `xray/*` (бухгалтерия
`dropped[]`, род группы `urltest` vs `group`). На CI корпус с гейтом
`existsSync` часть путей скипает — зелёный CI не доказывает паритет этих
кейсов.

### D7. CHANGELOG склеен с оборванными строками

**Серьёзность:** низкая (документация), но врёт про UI

В `[Unreleased]`:

```
22: - **Лист «N servers disabled» ведёт в детали узла на вкладке Diagnostics
30: - **Лист «N servers disabled» ведёт в детали узла на вкладке Notifications
270:- **Уведомления узла — внутри вкладки Diagnostics в деталях
280:- **Уведомления узла — отдельная вкладка в деталях
```

Строки 22 и 270 **оборваны** (нет закрывающей `**` и тела). Два
противоречащих абзаца про вкладку: истина после §501 — секция **внутри
Diagnostics снизу**, отдельной вкладки нет. Ноты и приёмка владельца это
подтверждают; CHANGELOG — нет.

Две спеки **§505** с одним номером — коллизия нумерации (правило AGENTS:
исполнители берут один и тот же «следующий» номер).

Спека 478 всё ещё пишет «вкладке Notifications» (`spec.md:145`) и указывает
хост как `screens/home/core_reject_host.dart` (`spec.md:380`); файл уже в
`services/core_reject/` (переезд отмечен в хвосте той же спеки, шапка не
догнала).

### D8. Виджет-тест §504 не влит

**Серьёзность:** низкая  
**Тип:** долг спеки 504, проверено

Файл лежит в `/tmp/lx_504/subscriptions_new_entry_highlight_test.dart`.
Зависал на `pumpAndSettle` из-за таймера 7 с и анимации. Поведение принято
вручную. Регрессия подсветки/прокрутки в CI не ловится.

### D9. `unknown_key` при разборе JSON по-прежнему дырявый канал

**Серьёзность:** средняя  
**Тип:** долг 460 W2e, уточнено

Санитайзер пишет `unknown_key` (`body_sanitizer.dart:389`), и шаг 1 фичи 472
гоняет его по **дословному** телу. Но модель — фильтр: ключ, который
`parseSingboxEntry` не читает, в `emit()` не попадает. Гард **сборки** видит
дословный JSON и ставит код; список подписки без пересборки — нет. Это
ровно D2: предупреждение сборки не переживает холодный старт.

Карточка узла с мусором «сразу при разборе» (CHANGELOG) верна для пути, где
санитайзер видит raw; не верна как общее правило для всех кодов гарда.

Уточнение по коду (`parse_warnings.dart`):

- узел конвейера (`isPipelineParsed`) второй проход по `emit()`
  **пропускает** — иначе `value` стал бы каноном (`chrome` вместо
  написанного `HelloChrome_120`);
- не-конвейерный узел судится по `node.emit()` — ключ вне схемы туда не
  попадает;
- `annotateFromRawBody` закрывает sing-box JSON; **Xray `rawSource` —
  объект Xray**, санитайзер его отказывает (`parse_warnings.dart:117–123`).
  У Xray-узла `unknown_key` на разборе по-прежнему только у гарда сборки,
  пока маппер не построит sing-box-карту.

### D10. Род AmneziaWG без уцелевших полей теряется

**Серьёзность:** низкая  
**Тип:** долг 480 §7, проверено текстом спеки

Узел, который просил AWG, но ни одно поле не уцелело, неотличим от обычного
WG: рода нет в каноне и в `NodeSpec`, эмиттер отдаёт `wireguard://`. Сейчас
безвредно (MTU уже 1280). Станет вредно, когда правило перейдёт с `any_set`
по телу на `when.source_kind`. Решения владельца нет.

### D11. Заголовок ARCHITECTURE.md отстаёт от конвейера

**Серьёзность:** низкая

Первая фраза: «parser and builder version is **v2** (spec 026)». Ниже по
файлу уже движок 480. Новый человек прочитает не ту норму.

### D12. Debug `GET /core_reject/notifications` смотрит в старую карту и `identical()`

**Серьёзность:** высокая  
**Тип:** новое (тот же класс, что §505)

`_warningsForCoreTag` (`handlers/core_reject.dart:266–290`) берёт узел из
`lastEmittedTagMap`, затем ищет его в хранилище через `identical()`. Дамп
без `tag` ключует подписки **хешем identity**, UserServer — **голым тегом**,
не config-тегом (`core_reject.dart:240–252`).

UI уже ходит в `storedNodeOfEmittedTag` / `warningsForConfigTag`. Debug эту
команду не делегирует — вторая реализация. После холодного старта или
переразбора `GET /core_reject/notifications?tag=🏠 awg2-home` даёт 404,
хотя вердикт в хранилище есть. Проверка 478 через Debug API по этому
эндпоинту врёт.

### D13. Экраны узла не слушают `entry` — кэш identity §283

**Серьёзность:** средняя  
**Тип:** новое

`NodeSettingsScreen` снимает `_node` и `_notifications` в `_load()`
(`node_settings_screen.dart:98–166`) и не подписывается на `widget.entry` /
контроллер. `NodeInspectScreen` получает инстанс `NodeSpec` и дальше читает
только его (`subscription_node_list.dart:245–246`,
`node_inspect_screen.dart:89`).

Память §283: экран, кэширующий identity нод подписки, обязан слушать
`entry` — фон (`AutoUpdater`, refresh, `_replaceList`) подменяет инстансы
мимо `loadNodes`. Открытая Diagnostics после обновления подписки или штампа
страховки держит старую секцию и точку на ярлыке, пока маршрут не закроют.

Список самой подписки по-прежнему слушает entry — тот фикс §283 не
откатили. Сломан только экран деталей.

### D14. Тело ↔ модель всё ещё рукописная вторая таблица

**Серьёзность:** средняя  
**Тип:** долг 480, проверено

URI / Xray / INI маппятся движком. `parseSingboxEntry` (`json_parsers.dart`,
switch по `type`) и `emit()` / `emitRaw()` (`node_spec_emit.dart`) — по-
прежнему перечень полей руками. `toUri()` уехал на движок; emit тела
sing-box — нет. Санитайзер сидит *вокруг* модели: слота нет — ключ реестра
молча не доезжает. Round-trip `parse(emit(body))` может потерять то, что
лаунчер держит.

Это не оверлей, а второй источник истины внутри приложения.

### D15. Два производителя предупреждений: реестр и рукописные классы

**Серьёзность:** средняя  
**Тип:** новое / хвост 460

В `node_warning.dart` живы подклассы (fingerprint, xhttp, obfs, detour,
groups, AWG3, packet encoding…). Дедуп — `warning_codes.dart` и
`_mergeRegistryWarnings` в `parse_warnings.dart`. Рукописный код побеждает
по `(code, path)` и может спрятать реестровый на том же поле. Debug отдаёт
тексты реестра; UI на рукописном классе может показать другую фразу.

### D16. Виджет Diagnostics тянет экран подписки

**Серьёзность:** низкая  
**Тип:** новое

`widgets/node_diagnostics_tab.dart:8` импортит
`screens/subscription_detail_screen/widgets/node_notifications_view.dart`.
Общий виджет нижнего слоя зависит от экрана. Тот же шов, что DNS/chains,
только появился в этом цикле (§501).

### D17. Кеш записи не используем: `body` и `warnings`

**Серьёзность:** средняя (нагрузка / батарея; не старт VPN)  
**Тип:** новое (владелец, 20.09.2026, разбор D2)  
**До тега v2.25.0:** не чинить — отдельная задача, если брать

В модели записи уже есть оба механизма кеша:

- `body` — распакованная заготовка узла в форме sing-box (кеш от
  `origin.raw`);
- `warnings` — поле записи (у подписки карта по узлу, у одиночного / члена
  папки — список).

Ими почти не пользуемся.

`origin.raw` — истина, это верно (§455: `body` для серверов **не пишется**,
при загрузке и при `body` в чужом бэкапе текст источника разбирают заново).
Предупреждения разбора живут только на объекте `NodeSpec` в памяти и после
рестарта **считаются снова** из `origin`. Поле `warnings` на диске занято
вердиктом страховки (`core_rejected` + `enabled`), не списком разбора и не
гардом сборки. Гард сборки — третья, уже вовсе не запись: карта
`lastBuildWarningsByTag` в контроллере, до следующей `generateConfig` или
смерти процесса.

Следствие: каждый холодный старт гоняет парсер по всем узлам, хотя объект
sing-box уже однажды получали; значки сборки на главном требуют ещё и полной
пересборки конфига (D2, D9). Это лишняя работа на CPU и не
энергоэффективно — механизмы кеша в контракте записи для этого и были.

Чинить — начать писать и читать `body` как кеш (с инвалидацией при смене
`origin` / бампе парсера) и складывать в `warnings` записи не только вердикт,
но и коды разбора/сборки, которые должны переживать рестарт. Норма «origin =
истина, body не истина» при этом не отменяется: кеш, не кнопка Regen.

---

## 5. Подтверждённые долги (не новые, живы)

Записаны авторами, при сверке на месте:

- Страховка **без UI** (Kotlin: boot / QS / сторож / Intent) и прочие
  Dart-старты мимо кнопки — спека 478, долг (1). **Принято 20.09.2026,
  не чинить** (разбор D1): автомат = жест Start; Kotlin — повтор файла с
  диска; новый конфиг без Start ждёт человека.
- Оверлеи в ноль: перенести `contract_draft/singbox/*` (13 файлов, наш
  вклад) в реестр лаунчера. → фича 480, §16 (§513).
- `dropped[]` += index и code — ждёт лаунчер. → фича 480, §16 (§513).
- Род `balancer_group` = тип тела sing-box.
- Q133-61 голый IPv6 в Endpoint `.conf` — два варианта владельцу. → фича
  480, §16 (§513).
- http `extra-headers` на входе (D133-18).
- Карточка warning с `text_<lang>` / cause / fix и якорь Learn more —
  хвост 460.
- DNS dual-write / `DnsController` → `screens/` — §295/§300, не этого
  релиза.
- Debug `chains.dart` импортит `screens/chain_edit/*` — старый шов.

---

## 6. До тега v2.25.0 (не дефекты кода)

Из pre-flight 19.09 и handoff:

1. ~~Вычитка нотсов, вынести `[Unreleased]` → `## v2.25.0`, закоммитить
   `docs(release): v2.25.0 notes`.~~ Сделано (пререлиз: ноты, архив, CHANGELOG,
   fastlane, статусы спек).
2. ~~Починить оборванные/противоречивые абзацы CHANGELOG (D7).~~ Сделано.
3. Прогон CI по актуальному `head_sha` после коммита нот.
4. ~~Приёмка владельцем на эмуляторе.~~ Принято 20.09.2026. Телефон — только
   по команде.
5. Issue #147 закрывается этим релизом — ответ EN после тега, отправляет
   владелец.

Не блокеры релиза, но дыры продукта: D2 и D12 (источник уведомлений в UI и
в Debug). D1 до тега не закрывать: вердикт 20.09.2026 — не чинить; в нотсах
формулировка про кнопку Start, не про любой подъём туннеля. D17 (кеш
`body` / `warnings`) — после тега, отдельной задачей.

---

## 7. Карта задач цикла (459–505)

Новые/существенно писавшиеся спеки задач с v2.24.4: 459, 461–477, 479,
481–505. Фичи: **460, 472, 478, 480**. Две спеки на номер **505**. Следующий
свободный номер после этой задачи — **507**.

Коммиты по меткам (грубо): 480 ≈ 108, 472 ≈ 26, 478 ≈ 24, 460 ≈ 6, прочее —
синки, ядро, UI 496–505.

---

## Верификация

Читение кода и спек на `6d3fe599`. Тесты и analyze не гонялись: правок
кода нет. Находки D1–D17 — адреса в исходниках на эту голову. D17 —
решение владельца по разбору D2, не правка кода.
