# §510 — Находки независимого ревью после v2.25.1

| | |
|---|---|
| **Статус** | **Released в v2.25.2** (24.09.2026). Готово: H1, M2, M3, L1, L2, M1, m2, m3 починены; m1 — реестр лаунчера (сторож со skip) |
| **Дата** | 2026-09-24 |
| **Источник** | независимое ревью `develop` после v2.25.1: блоки «страховка 478 / сборка / Debug API» (H1, M2, M3, L1, L2) и «движок разбора» (M1, m1–m3) |
| **Связанные** | фича [478](../features/478%20core-rejected-node-auto-disable/spec.md), задачи [494](494-debug-api-debts.md), [503](503-core-reject-list-disabled-node-navigation.md), §208 (`/pool`), фича 480 (движок) |

## Docs to update

- эта задача — таблица находок
- [478](../features/478%20core-rejected-node-auto-disable/spec.md) — «Как
  сделано → Автомат» (H1), «Связка и контроллер» (M2)
- [503](503-core-reject-list-disabled-node-navigation.md) — ключ члена
  папки и поиск по причине (M3)
- [CHANGELOG.md](../../../CHANGELOG.md) — Unreleased → Fixed, строка на
  каждый видимый дефект

## Правило приёмки

Каждая починка — отдельный коммит с тестом, который КРАСНЫЙ на прод-файле до
правки и зелёный после (проверено откатом прод-файла). Идентичность и тела
существующих фикстур не меняются: стражи `before_480_identity_snapshot`,
`engine_emit_shape`, `golden_config` зелёные на каждом шаге.

## Находки

| Находка | Воспроизведена | Фикс / отказ |
|---|---|---|
| **H1** — защита от зацикливания страховки по строке тега: у тёзок `Dup`/`Dup-1` после выключения первого тег `Dup` переезжает ко второму, прогон обрывается, VPN не поднимается | да: реальный `buildConfig` отдаёт второму литеральный `Dup`; автомат — `failed` на первом круге | повтор судится по `CoreRejectNodeRef` из `disableNode` (`_seenRefs`), а не по тегу. Тест `core_reject_retag_test.dart` (хост поверх настоящей сборки): оба тёзки выключены за один прогон, VPN поднят |
| **M2** — Stop мимо кнопки фазы цикла (`POST /action/stop-vpn`, плитка QS, Intent API, Locale) не гасит идущий прогон: автомат доводит цикл до чистого `check` и финальным стартом поднимает туннель через секунды после Stop | да: Stop в фазе `checking` → `startedWithDisabled`, два реальных старта | `HomeController.stop()` зовёт `CoreRejectState.cancelRun()` (кнопка Stop, `/action/stop-vpn`); нативная воронка `BoxVpnService.stop` шлёт в Dart `vpn-stop-requested` (`VpnPlugin.notifyStopRequested` → `automation_dispatcher`) → тот же `cancelRun`. Кнопка Stop в шторке ходит в сервис напрямую (`ACTION_STOP`), но шторка живёт только в фазах реального старта, где Stop и так кончает прогон (`realStart` → unavailable → `failed`, без следующего старта). Тесты в `home_core_reject_test.dart`: оба пути, `realStarts == 1`, прогон не активен |
| **M3** (§503) — узел строки листа страховки искался по тексту причины (первый вердикт с тем же текстом), а ключ члена папки был голым тегом: при одинаковых причинах / тёзках в папке тап открывал чужой узел | да: подписка `Alpha`/`Beta` с одной причиной → открывается `Alpha`; тёзки в папке — одинаковый `node_key`, открывается первый член | ключ члена папки — сырой тег, уникализированный по папке (`folderMemberKeys` = `sourceNodeRawTags`, адрес NODE_LINK §2.2; у уникального имени ключ прежний, старые вердикты читаются, голый тег — запасной путь `_resolveByRef`); ref по тексту причины берётся, только если причина однозначна, иначе поиск по тегу строки. Тесты `core_reject_resolve_test.dart`: одна причина на двоих, тёзки в папке (красные до правки), две папки / две подписки с одинаковыми тегами и причинами (сторожа) |
| **L1** — `/pool` есть в тексте `/help`, но не в `/help?format=json`: инструмент, строящий список путей по JSON, считал роут несуществующим | да: префиксы JSON — 22 из 23 смонтированных, без `/pool` | строка `/pool` в JSON; `Router.prefixes` и тест в `help_json_test.dart`: каждый смонтированный префикс описан в JSON (следующий новый роут без строки в help упадёт тестом). В CHANGELOG не идёт — поверхность разработчика |
| **L2** — `POST /action/start-vpn` без флага требовал контроллер подписок ДО ветки `guard`, хотя путь `guard=false` его не касается: 409 `subscription controller not ready` там, где до §494 был старт | да: без `sub` в реестре → `Conflict` | при `guard=false` — `home.start()` без `requireSub`, как до §494; подписки требуются только при `guard=true`. Тест `action_start_vpn_test.dart`: 200 и `startVPN` без контроллера подписок. В CHANGELOG не идёт — поверхность разработчика |
| **M1** (движок) — `item_pattern` судил только строковые элементы: число в `tls.alpn` / `hysteria2.server_ports` (форма `listable_string` пускает `String\|num`) проходило гейт сборки и роняло ВЕСЬ конфиг ошибкой unmarshal ядра | да: гейт на `alpn: [443,"h2"]` и `server_ports: [443]` — без изменений и без кодов | нестроковый элемент в цикле `drop_item` (`body_sanitizer.dart`) снимается тем же кодом элемента `<field>_item_invalid`, путь `field[i]`, значение как написано. Число НЕ приводится к строке: ядро читает `Listable[string]`, а `"443"` — не ALPN и не пара портов (`alpn: [443,"h2"]` → `["h2"]`). Тест `registry_gate_item_pattern_test.dart` (гейт + вставка sing-box JSON): красный до правки. Фикстуры с числовыми элементами не найдены; стражи идентичности / формы эмиттера / golden зелёные |
| **M2** (движок) — `item_pattern` у `hysteria2.server_ports` пропускает порты вне 0..65535 | не входило в задание | реестр лаунчера, у нас не чинится |
| **m1** — у hysteria v1 `server_ports` в реестре нет `item_pattern` / `on_item_invalid`: адрес в элементе уходит в ядро | да (по реестру) | не чиним: правка в реестре лаунчера, запрос отправлен. Сторож со `skip` и причиной — в `registry_gate_item_pattern_test.dart`, снимается, когда реестр догонит |
| **m2** — `matches` / `not_matches` в условиях компилировались `RegExp` напрямую, мимо общего `_regex`: без перевода Go-группы `(?P<name>` и без кэша | да: условие с `(?P<…>)` — `FormatException: Invalid group` | через `_regex` (`interpreter.dart`). Тест `engine_matches_regex_test.dart` на синтетической секции: красный до правки. Сегодня в реестре только тривиальные выражения — видимого дефекта нет, в CHANGELOG не идёт |
| **m3** — `_mergeOverlay` отличал группу от записи только по отсутствию `source` у оверлея: запись оверлея без `source` при одноимённой записи реестра сливалась с ней, и у записи оставались `source`/`implies` реестра вопреки «замещает целиком» | да: синтетическая пара — у слитой записи остаются `source` и `implies`. На текущих оверлеях не проявляется | фикс локальный: запись — если `source` есть у ЛЮБОЙ стороны (`section_loader.dart`). Тест `section_overlay_merge_test.dart`: кейс замещения красный до правки; снимок — все оверлеи `assets/contract_draft/**` (секции и общие блоки) сливаются новым правилом ровно как старым |
