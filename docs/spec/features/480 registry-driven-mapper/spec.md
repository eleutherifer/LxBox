# 480 — маппер от реестра: в коде остаётся только движок

| | |
|---|---|
| **Статус** | **Released в v2.25.0** (20.09.2026, ядро `v1.14.1-lx.8`). Реализовано (W0–W8), зеркало реестра — контракт **1.1.46** (синк 19.09.2026, лаунчер `7e2945bf`; что вышло — разделы 14–15). Рукописные мапперы ссылок, Xray и INI и рукописная сборка ссылки заменены движком `app/lib/services/parser/engine/`, исполняющим секции реестра; в коде не осталось ни одного имени схемы (страж `engine_no_scheme_names_test`). Что сознательно отложено — раздел «Что осталось» волн W5–W6; открытые расхождения с лаунчером — раздел 12. |
| **Дата** | 2026-09-19 |
| **Контракт** | 1.1.11 (зеркало `app/assets/contract/`, оверлеи `app/assets/contract_draft/`) |
| **Источник** | решения владельца 18–19.09.2026: «проверки не вручную, а управлялись конфигами… если не хватает таблиц — сделать более глубокое ветвление»; «дать реестру нужное количество примитивов, в коде остаётся только движок, общий для всех схем»; «без этого релиза не разрешаю, так же и в LxBox»; «сборку ссылки по таблице реестра делаем в этом релизе» |
| **Норма** | лаунчер `SPECS/133-F-N-REGISTRY_DRIVEN_LINK_MAPPER/` (`SPEC.md`, `PRIMITIVES.md`, `SCHEMES.md`, `TASKS.md`, `DELTAS.md`), первый вариант — `bfe65293`; реестр — `contract/registry/` |
| **Связанные** | фича 472 (единый конвейер: маппер → санитайзер → модель — эта фича заменяет его первый слой), фича 460 (реестр в приложении), §476 (страж круга полей тела), §454–§456 (`rawSource` — форма хранения), фича 478 |

## 1. Зачем

Санитайзер уже устроен правильно: один движок (`body_sanitizer.dart`) исполняет
атрибуты `body.fields` реестра и не знает ни одного протокола. Новое правило
значения — правка JSON у лаунчера и синк, а не код в двух приложениях.

Слой перед ним устроен наоборот. 13 рукописных мапперов ссылок
(`app/lib/services/parser/mappers/*_mapper.dart`), маппер Xray, маппер INI,
рукописное опознание формата документа (`isDirectLink`, развилки
base64 / список ссылок / JSON / INI) и рукописная сборка ссылки
(`node_spec_emit.dart`, `toUri*`). Секции `uri`, `mapper`, `emit` в реестре
есть, но их читает только генератор документации. Каждое расхождение
Go ↔ Dart в этом слое — отдельная находка и отдельная заплата с двух сторон.

Маппер по сложности не выше санитайзера: таблица «откуда взять → куда
положить» плюс конечный набор структурных примитивов. Объём работы — не в
движке, а в доказательстве, что ни один живой узел не изменился.

## 2. Решения (приняты)

1. **Реестр управляет превращением ВСЕХ источников в узел.** У протокола —
   раздельные секции-мапперы по виду источника: ссылка, Xray-JSON,
   sing-box-JSON, `.conf`/INI. Примитивы и движок общие.
2. **sing-box-JSON — не «как есть», а маппер с диалектами форков**: опознание
   диалекта по признакам тела, алиасы и пути по диалекту, перенос
   outbound ↔ endpoint, `peers[]` ↔ плоские поля; неизвестный ключ — info-код.
   Xray-JSON — тоже диалекты. *Граница:* хранение не меняется — дословный
   `rawSource` (§454–§456) остаётся источником; маппер работает только на пути
   в модель.
3. **Декларативное опознание, два уровня.** Реестр видов документа
   (base64-подписка, список ссылок, конфиг xray, конфиг sing-box, ini, …) и
   `detect` у каждой секции-маппера (regex и/или json/ini-схема). Рукописных
   снифферов формата в коде не остаётся. Линтер: каждый кейс корпуса
   опознаётся ровно одной секцией.
4. **Сборка ссылки — от той же таблицы, в этом релизе**, последней волной
   после доказанного разбора.
5. **Декодирование.** Percent-декод query — один раз. «+» в query — пробел
   (норма form-encoding не отменяется); у полей base64-формата (ключи
   WG/AWG/masque, `pbk`, `header_protection_key`) «+» читается буквально —
   свойство формата поля в реестре, заплат в коде нет. `alpn` — повторный
   percent-декод до стабилизации, не более 16 проходов; `path` — 2 прохода,
   повторный — только percent. На выходе пробел — `%20`.
6. **Имена параметров — регистронезависимо**; при двух написаниях побеждает
   точное совпадение с каноном, иначе первое по порядку.
7. **Неизвестный параметр источника — `uri_param_unknown` (info).**
   «Объявленными» считаются и параметры общих файлов (tls, transports,
   dialer); содержимое `extra` у xhttp — не параметры ссылки.
8. **Тег безымянного узла — по типу узла** (`hy2://` → `hysteria2-…`,
   `ss://` → `shadowsocks-…`), правило в реестре. У нас так сейчас —
   identity не двигается. Xray-вход держит историческое написание — в секции
   xray.
9. **Authority разбирает лексер движка**, не `Uri.parse`: платформенный
   парсер отказывает на `host:443,20000-30000`.
10. **Дельты поведения — только явным списком** (`DELTAS.md` у лаунчера,
    «было → стало» для обоих приложений), отдельный коммит с правкой
    ожиданий на каждую. Критерий отбора (владелец доверил решать
    исполнителю): тело РАБОЧЕГО узла и identity не меняются; чинится сегодня
    сломанное или молча теряемое; общепринятая норма не отменяется;
    исключения — атрибутом реестра. Позиция LxBox: регистр имён — да;
    `pinSHA256` списком по запятой — да; пароль trojan по «:» не резать;
    открытый userinfo SS2022 читать; эвристику SNI у trojan не включать
    (если лаунчер оставит её как `default_from` — решить по фикстурам);
    9 написаний `insecure` — да; vmess legacy с IPv6 в скобках — починить.

## 3. Состав

| Часть | Что | Вместо чего |
|---|---|---|
| Движок маппера | исполняет секции-мапперы: пространство источников (userinfo, host, port_raw, query, fragment, json-путь, ini-ключ), примитивы раздела 4 | `mappers/*_mapper.dart`, `xray_mapper.dart`, `mapWireguardIni` |
| Лексер входа | текст ссылки → пространство источников, без `Uri.parse`; декодеры по правилам §2.5 | `Uri.queryParameters`, `queryParamPreservePlus`, `decodeResidualPercent`, `_decodeAlpnElement` |
| Опознание | вид документа → секция → диалект, по `detect` реестра | `isDirectLink`, развилки формата в разборе подписки, `kPipelineSchemes` |
| Эмиттер ссылки | обращение той же таблицы: `maps_to⁻¹`, `value_map⁻¹`, `compose`, `param_order`, `omit_default`/`emit_when` | `toUri*` в `node_spec_emit.dart`, `transportToQuery` |
| Стражи | греп «нет имён схем в пакете маппера»; линтер однозначного опознания; круг `parse(emit(node))`; identity-фикстуры | `mapper_rules_coverage_test` (`_knownGaps` исчезает) |

Не меняются: санитайзер и `body.fields`; `parseSingboxEntry` и модель
`NodeSpec`; форма хранения; UI и видимые строки (кроме текстов нового кода из
`warnings.json`).

## 4. Грамматика секций

Нормативна SPEC 133 лаунчера (`PRIMITIVES.md`). Раздел дописывается после
расширенной спеки: перечень примитивов с Dart-семантикой каждого и таблица
«наше рукописное место → примитив».

Запросы LxBox к грамматике, принятые лаунчером как обязательные до кода
движка (сверка 19.09.2026, 40 мест по 13 мапперам):

| # | Примитив | Наше место |
|---|---|---|
| G1 | условие по ИСТОЧНИКУ/форме, не по телу | род AWG объявляет вход (`kindIsAwg`), когда в теле не осталось awg-ключей |
| G2 | `sets` умеет СНИМАТЬ путь (`null`) | tuic `disable_sni=1` убирает `tls.server_name` |
| G3 | приоритет двух записей в один путь | `flow=…-udp443` побеждает явный `packetEncoding`; `scheme_sets` anytls против `security` |
| G4 | `sort_keys` у объекта | заголовки http/naive — порядок ключей входит в тело |
| G5 | `flatten` вложенного объекта | `extra.xmux.*` у xhttp |
| G6 | `extract` для SIP003 | ss `plugin` → `plugin` + `plugin_opts`, алиас `pluginOpts` |
| G7 | INI: источник-комментарий, Endpoint с голым IPv6 | имя из комментария под `[Peer]`; «несколько `:` без скобок = IPv6, порт по умолчанию» |
| G8 | нормализация непустой метки | strip_control + trim + `🇪🇳→🇬🇧`; метка = identity |
| — | «материализовать default» в маппере | vmess `security` у Xray-входа; дефолты masque (`profile`/`vhttp`/`mtu`) |
| — | `drop_node` у полей WG | битый `presharedkey`, AWG3-паддинг < 12, битый `header_protection_key` (контракт 1.1.11) |

## 5. Критерии приёмки

1. Identity-фикстуры `app/test/fixtures/<схема>/pipeline_identity_before.json`
   всех схем и входов — без единого изменения, кроме кейсов из `DELTAS.md`;
   каждая дельта — свой коммит с «было → стало».
2. Круг `parse(emit(node))` даёт то же тело на всех фикстурах; ссылки,
   сохранённые прежними версиями (`rawSource` ручных узлов), читаются в то же
   тело. Тег не двигается ни у одного узла.
3. Греп-страж: в пакете маппера нет имён схем и нет схемных функций;
   `*_mapper.dart` удалены.
4. Линтер: каждый кейс корпуса опознаётся ровно одной секцией; рукописных
   снифферов формата нет.
5. Корпус контракта зелёный без новых per-app override'ов.
6. Перф: полная воронка 2000 узлов — не дороже ×1,5 к замеру до фичи
   («лучший из трёх»).
7. Без реестра (на CI каталога `app/contract` нет) тесты грузят зеркало
   `app/assets/contract`; движок без реестра не работает вовсе — рукописного
   запасного пути не остаётся, отсутствие реестра в сборке — ошибка сборки
   (страж `check_contract_lock`).

## 6. Волны

| Волна | Что | Страж |
|---|---|---|
| W0 | досъёмка фикстур «до» старым путём: trojan файлом, ss+plugin, сырой «+», регистр имён, tuic `disable_sni`, ssh PEM, socks4/4a, vmess legacy IPv6, INI, фолбэк тега | — (идёт) |
| W1 | лексер + движок + пилот trojan; рукописный trojan-маппер удалён | фикстура trojan, перф |
| W2 | vless + общие tls/transports (самое большое покрытие примитивов) | 89 кейсов vless |
| W3 | vmess (две формы), ss, socks/http/naive/anytls/ssh | фикстуры схем |
| W4 | hysteria2, tuic, masque, wireguard/awg ссылкой | фикстуры схем, WARP |
| W5 | входы-документы: INI, Xray-JSON (диалекты), sing-box-JSON (диалекты) | фикстуры xray/wireguard, §476 |
| W6 | опознание: реестр видов документа + `detect`; снятие `isDirectLink` и развилок | линтер однозначности |
| W7 | сборка ссылки от таблицы; удаление `toUri*` | круг `parse(emit)`, чтение старых ссылок |
| W8 | удаление остатков, греп-страж, доки (ARCHITECTURE, PROTOCOLS, GUARDS), CHANGELOG | — |

Порядок волн W1–W4 следует `TASKS.md` лаунчера: секция реестра для схемы
приходит синком контракта, наша волна — после неё. Каждая волна: исполнитель —
Opus-агент по ТЗ; локально `flutter analyze` + свои тесты; CI — отдельным
агентом-дежурным.

## 7. Что вышло: W1

Движок и пилот trojan влиты в `develop`. Единый документ устройства движка —
`MAPPER_ENGINE.md` лаунчера (раздел НОРМА плюс приложение «Dart», куда уехала
наша записка W1); в этом репозитории его зеркала нет. Всё, что относится
именно к НАШЕЙ реализации, живёт ниже, разделами «Что вышло» по волнам.

**Пакет `app/lib/services/parser/engine/`** — пять частей и ни одного имени
схемы (греп-страж `engine_no_scheme_names_test.dart` ловит слово даже в
комментарии):

| Файл | Что делает |
|---|---|
| `lexer.dart` | текст → пространство источников без `Uri.parse`: переживает `host:443,20000-30000`, IPv6 в скобках, сырой userinfo; query — список пар, потому что при двух написаниях имени норма требует «первое по порядку» |
| `decoders.dart` | две семантики percent-декода (`query` с `+`→пробел, `path` с литеральным `+`) и N проходов либо до стабилизации; какую применить — решает запись, а не код |
| `source_space.dart` | плоское пространство имён: `query.*`, `userinfo*`, `host`, `port_raw`, `path`, `fragment` плюс `json.*`/`ini.*` под волну W5 |
| `section.dart` | модель FROZEN-грамматики; DRAFT-имена (§0.7) изолированы в `DraftNames` |
| `interpreter.dart` | исполнение: `scheme_sets` → `defaults` → userinfo → селекторы → зависимые → `default_from`/`materialize_default` → метка → `unknown_key` |
| `section_loader.dart` | секция из реестра, если исполняемая, иначе из черновика; `include` разворачивает блоки по диалектам |

Данные — `app/assets/contract_draft/uri/`: копии ИСПОЛНЯЕМЫХ секций
лаунчера (контракт 1.1.13, `8cc6ac69`), а не наше сочинение. Черновик
временный: как только секция приедет синком контракта, загрузчик возьмёт
её оттуда без правки кода.

**Расширения грамматики.** Заводить своих имён не пришлось: G1–G8 вошли в
FROZEN-таблицу лаунчера (`PRIMITIVES.md` §0, `4a3dbf3f`) до начала работы.
Сверх неё понадобилось четыре ЗАПИСИ (не примитива) и одно уточнение нормы
— всё передано лаунчеру списком:

| Что | Зачем |
|---|---|
| `transports#uri.ws.ed` / `.eh` | плоская форма `?ed=N&eh=` параметрами ссылки: у лаунчера объявлен только хвост пути, и узел молча терял `max_early_data` |
| `tls#uri.ech` | `maps_to: null` + `on_present` — ECH объявленно никуда не едет; без записи параметр уехал бы в `uri_param_unknown` |
| блок `dialer#uri` | §453 keep-alive тремя плоскими записями; у лаунчера блока нет |
| сила `defaults` секции | грамматика молчит, кто побеждает: дефолтный порт или явный. У нас дефолт слабее любой записи — иначе `443` перебивал бы порт из ссылки |

Отступление от секции лаунчера одно: `pbk`/`sid`/`key_share` сужены
условием по типу тела. Общий `tls#uri` отдал бы REALITY и trojan, то есть
ДОБАВИЛ бы блок телу рабочего узла, а дельты на это нет ни в `DELTAS.md`,
ни в ТЗ волны. Снятие сужения — отдельной дельтой.

**Дельты (5 кейсов из 35).** Каждая помечена в фикстуре полем `_delta480`,
прежние значения лежат рядом в `_before480`, и тест проверяет оба
направления — тело обязано совпасть с новым ожиданием И отличаться от
прежнего:

| Кейсы | Было | Стало |
|---|---|---|
| `upper_type_ws`, `upper_alpn`, `upper_fp` | `Type=`, `ALPN=`, `Fp=` не читались — транспорт, ALPN и отпечаток терялись молча | читаются (норма §0.6: имена регистронезависимо) |
| `path_raw_plus`, `path_encoded_slash_raw_plus` | `/a b` — путь декодировался query-семантикой, сервер отвечал 404 | `/a+b` — path-семантика на обоих проходах (D133-14, чинится у обеих сторон) |

`upper_security` и `upper_sni` дельтами НЕ помечены: тело у них не
меняется. `Security=tls` совпадает с дефолтом схемы, `SNI=` — с
`default_from: host`, то есть непрочитанный параметр давал тот же ответ
случайно, и пометка прикрывала бы будущую регрессию.

**Перф** (2000 ссылок trojan, лучший из трёх, разнородный набор из пяти
форм): слой маппера 18 → 75 мс (×4,06), полная воронка +28%. Критерий
спеки — ×1,5 на воронке — выдержан: маппер это 37% воронки, остальное
санитайзер и постройка модели. Четырёхкратность слоя снимается
предкомпиляцией плана (`_orderedParams` и разбор строк-источников идут на
каждом узле); делать её стоит на волне, где движок понесёт 13 схем.

**Тестовая обвязка волны** — четыре разных судьи, и каждый закрывает своё:

| Судья | Что держит |
|---|---|
| `before_480_identity_snapshot_test` + `engine_<схема>_pilot_test` | сверка со СТАРЫМ путём: identity-хеш, тег и тело БАЙТ В БАЙТ, включая порядок ключей. Формат фикстур — `test/fixtures/<схема>/pipeline_identity_before.json`, `cases.<имя>: {uri, identity, tag, body}`; у отбракованных `identity: null` + `dropped: true` (узел, которого сегодня нет, появившись, влез бы в подписку новым) |
| `engine_delta_dump_test` | новые ожидания дельт снимаются ПРОГОНОМ, а не вписываются руками: снимок описывает поведение, а не намерение |
| `engine_no_scheme_names_test` | греп-страж: в `engine/` нет ни одного имени протокола, включая комментарии. Из-за него список файлов черновика живёт вне пакета (`mappers/draft_sections.dart`) |
| `test/contract/mapper_sections_draft_test` | страж ФОРМЫ секций, написан раньше движка соседней волной: замороженные ключи, `source` у каждой записи, ровно одна `default`-форма, компилируемость `extract.re` |

Гейт у всех — зеркало `app/assets/contract`, а не вендоренная копия
`app/contract`: последней на CI нет вовсе, и под её гейтом тесты молча
пропускались бы ровно там, где нужны.

## 7c. Что вышло: W2 (vless, контракт 1.1.16, предкомпиляция)

**Синк на 1.1.16** (`64377c76`) закрыл четыре из шести отступлений, которые
W1 держала оверлеями. Закрыл их лаунчер, а не мы: каждое было отправлено ему
красным кейсом, и ответ пришёл правкой реестра.

| Отступление W1 | Чем закрыто в 1.1.16 |
|---|---|
| `tls#uri.pbk` сужен по типу тела | `pbk` вынесен в отдельный блок `tls#uri_reality`; подключают его только vless и anytls, и trojan REALITY больше не получает |
| `ws.path` без кода `ws_early_data_converted` | код объявлен атрибутом `code` у ГРУППЫ `extract.into` — не у записи целиком: хвост разложился по двум полям, и сказать об этом может только тот член, который его поймал |
| `httpupgrade.host` с откатом на `sni` | откат снят, цепочки больше нет |
| `uri_param_unknown` у trojan снят целиком | §3B.2: записи-`null` и `overlay` считаются объявленными, и код перестал красить корпус |

Осталось одно отступление W1 и появилось одно новое — оба ниже, в «Оверлеях».

**vless на движке.** 99 кейсов снимка, **6 дельт, и все шесть уже приняты**
на trojan и W4 — своих у vless не появилось:

| Кейсы | Было → стало | Основание |
|---|---|---|
| `upper_type_ws`, `upper_alpn`, `upper_fp` | `Type=`, `ALPN=`, `Fp=` не читались | §0.6 FROZEN, регистр имён |
| `path_raw_plus`, `path_encoded_slash_raw_plus` | `/a b` вместо `/a+b` | D133-14 |
| `pbk_raw_plus` | `+` становился пробелом, ключ не проходил санитайзер и блок `reality` пропадал ЦЕЛИКОМ | D133-7 |

Последняя дельта чинит больше прежних: у trojan тот же дефект отнимал одно
поле, здесь — весь блок REALITY у рабочего узла.

**Сверка научилась проверять дельты двусторонне.** Раньше это умели только
пилот и W4-тест, а `engine_w4_reconcile_test` (он идёт по ВСЕМ кейсам файла)
сравнивал тело с ожиданием и на пометку не смотрел. Односторонняя пометка
протухает молча и с этого момента прикрывает регрессию — теперь тело обязано
совпасть с новым ожиданием И отличаться от `_before480`.

**Гейты тестов vless переведены на зеркало.** `app/contract` на CI нет, и под
её гейтом тесты молча пропускались бы ровно там, где нужны. Корпус при этом
остался на `app/contract` СВОИМ гейтом: в зеркало едет один `registry/` (оно
бандлится в APK), и общий гейт погасил бы вместе с корпусными тестами и
тесты разбора.

**Предкомпиляция плана** — долг №1 записки W1 и рекомендация НОРМЫ. На каждый
узел считались раскладка записей на проходы A/B, сортировка по `priority` и
множество объявленных написаний, хотя секция между узлами не меняется. План
(`_SectionPlan`) считается один раз и кэшируется по ЭКЗЕМПЛЯРУ секции через
`Expando`: перезагрузка реестра даёт новый экземпляр и новый план вместе с
ним, без ручной инвалидации. Пути тела раскладываются на сегменты один раз
(`_segments`); json-пути кэш не трогает — там пространство ключей не
ограничено секцией.

| Замер (2000 vless, лучший из трёх) | Было | Стало |
|---|---|---|
| слой маппера | 213 мс | 175 мс (−18%) |
| полная воронка | 369 мс | 347 мс |

`engine_perf_test` печатает теперь слой и воронку ОТДЕЛЬНО: критерий спеки
задан на воронке, но дорожает слой, и по одному числу не видно, что именно
подорожало.

Осталась одна непредкомпилированная статья: вид источника читается разбором
строки-имени (`startsWith('query.')`) на каждом обращении, хотя он известен
при загрузке записи. Снимается полем у `MapperParam`; на W2 не делалось,
чтобы не трогать `source` одновременно с волной секций.

**Два решения устройства, сознательно не менявшиеся.**

- **Два прохода, а не топологическая сортировка.** Сперва `selector: true`,
  затем остальные — ограничение то же, что у Go, и названо у обеих сторон
  спорным: `when` на путь, который пишет НЕ селектор, работать не будет. Ни
  на одной волне не помешало; если помешает, честнее объявить третий проход
  в грамматике, чем добавить его молча.
- **`SourceSpace` остался ОДНИМ классом.** Записка W1 предлагала развести
  его на интерфейс с реализациями url/json/ini — ровно потому, что
  `json`/`ini` тогда не заполнялись ни разу. После W5–W7 заполнены и
  читаются оба: исходный довод («поля мёртвые») исчез, а оставшийся —
  вкусовой. Вернуться стоит, если у пространства появится ЧЕТВЁРТАЯ форма.

### Оверлеи, оставшиеся после 1.1.16

Оба — красные кейсы, переданные лаунчеру. Снимаются его ответом, не молча.

| Оверлей | Кейс | Чего ждём от реестра |
|---|---|---|
| `transports#uri.ws.ed` — `implies` НЕ ставит `early_data_header_name` | плоская форма `?ed=N` без хвоста пути | лаунчер пишет имя заголовка НАМЕРЕННО (его код), у нас по §103 D-008 дефолт применим только когда early data взята из ХВОСТА ПУТИ. Расхождение признано обеими сторонами, ждёт общего кейса корпуса. Тело живых узлов — наша сторона |
| `vless.include` + `dialer#uri` | `§453 dial-поля идут мимо санитайзера` | секция реестра не подключает `dialer#uri`, хотя блок в реестре есть и его записи объявлены. У нас keep-alive читается параметрами ссылки с §453, и без подключения три поля терялись бы молча вместе с настройкой человека. Тот же оверлей, что у trojan |

### Новые атрибуты грамматики в движке (1.1.16)

Своих имён не заводилось — все из FROZEN-таблицы лаунчера:
`on_when_false`, `on_implies_written`, `value_map_case: sensitive`,
`userinfo.required`, работающий `on_invalid: keep`, `code` у члена
`extract.into`, `forms[].decode` с областью (`scope: userinfo|authority`).

Два решения реализации, которые стоит знать второй стороне:

- **`on_when_false` спрашивает источник, НЕ отмечая его прочитанным**
  (`_valueOfBare`, норма §10.2). Иначе подавленный условием параметр переставал
  бы давать `uri_param_unknown` — код замолкал бы там, где он единственный
  признак непонятого входа.
- **`on_implies_written` смотрит на тело ДО и ПОСЛЕ**, а не на факт вызова:
  при занятом пути присваивание проигрывает владельцу, и сообщать не о чем.
- **`scope`-декодер пересобирает ссылку и зовёт лексер второй раз**, а не
  правит поля пространства: декодированный authority приносит СВОЮ структуру
  (`base64(method:password@host:port)` — это и userinfo, и хост, и порт
  разом), и разбирать её обязан тот же лексер, а не второе место с теми же
  правилами.

## 7a. Что вышло: W5–W6 (входы-документы и опознание)

Три блока в `develop`: Xray-JSON на движке, опознание вида документа от
реестра, секции вида источника `singbox`.

### Xray-JSON

`mappers/xray_mapper.dart` **удалён**; `json_parsers.dart` зовёт движок.
Второй вход движка — `runSectionOnJson`: пространство строится не лексером
из текста, а из уже разобранной карты (норма §1 — «JSON элемента
разбирается один раз на элемент»; просить движок разбирать текст заново
значило бы разбирать подписку из 2000 узлов дважды). `$base` подставляется
якорем формы, и одна таблица обслуживает `settings.vnext.0`,
`settings.servers.0` и плоскую форму.

**Диспетчера по имени протокола не осталось.** Секцию выбирает `detect`
самой секции (`MapperSections.matchJson`): прежний `switch (protocol)` и
был опознанием элемента, просто в коде.

Сверка — `engine_xray_pilot_test.dart`: 45 входов снимка
`xray/pipeline_identity_before.json` сходятся **байт в байт** (identity,
тег, имя, `rawSource`, тело), плюс линтер «ровно одна секция на элемент».
Два расхождения со снимком (`vless_ws_path_junk`, `vless_encryption_junk`)
объявлены раньше, шагом 8 фичи 472.

**Дельта волны — одна, `vless_default_port`** (19.09.2026). Дефолт порта
443 на Xray-входе снят: было — узел на 443, стало — ноль узлов. Арбитром
по решению владельца выступили исходники XTLS/Xray-core, и они показали,
что расхождение сторон было ложной дилеммой — **дефолта порта у Xray нет
ни у одного outbound-протокола**. `trojan` и `shadowsocks` отбраковывают
элемент явно (`infra/conf/trojan.go:67-69` — `Invalid Trojan port.`;
`infra/conf/shadowsocks.go:223-224,247-248`), а `vless`/`vmess`/`socks`/
`http` порт не проверяют вовсе (`infra/conf/vless.go:274-283`): поле
объявлено голым `uint16`, отсутствующий ключ даёт 0, элемент собирается и
падает при первом дозвоне. Рабочего узла из элемента без порта не выходит
ни в одной ветке — то есть наш дефолт был единственным из трёх поведений,
который ПРИДУМЫВАЛ узел, которого провайдер не присылал. Пометка
двусторонняя: `_delta480` у кейса фикстуры плюс `_expectedChanges` в
`engine_xray_pilot_test` и `xray_pipeline_invariants_test`.

Остаток, дельтой не являющийся: причину отбраковки мы не показываем —
`desc_en` записи наш движок не читает, `required` роняет узел молча
(`interpreter.dart:1198-1211`). Свойство ВСЕХ наших записей `required`, а
не порта; лаунчер причину называет. Отдельная задача.

Отступления от секций реестра оформлены **оверлеями**, каждое — красный
кейс, переданный лаунчеру:

| Отступление | Было → стало без него |
|---|---|
| `tls#xray.alpn` → `maps_to: null` | запись реестра ДОБАВЛЯЕТ `tls.alpn` телу; с этого входа ALPN у нас не ехал никогда (`vless_alpn_tls`). D133-C9 у лаунчера помечен КАНДИДАТОМ, а запись уже боевая |
| `include` + `dialer#xray` | в `include` реестра блока нет — keep-alive из `sockopt` (§453) терялся (`vless_sockopt_keepalive`) |
| ~~`defaults.server_port: 443`~~ | **СНЯТО 19.09.2026 по арбитру Xray** (дельта `vless_default_port` выше). Было наше отступление: у реестра `port` `required` без дефолта. Арбитр показал, что дефолта нет и у самого Xray, — сторона реестра верна, оверлей убран у `vless`/`vmess`/`trojan`, у `socks` вместе с ним ушёл дефолт 1080 |
| `forms[].detect.type_of` вместо `default: true` | из мусорного `streamSettings: "none"` собирался рабочий узел без транспорта и TLS, а сосед по элементу задваивался (`malformed_stream`) |
| `$sni_from_server`, `when: {tls.enabled}` у `fp`/`insecure`/`pbk` | имя сервера не заполнялось адресом узла; блоки TLS появлялись при `security: none` |

### Опознание вида документа

Реестр `assets/contract_draft/source_kinds.json` плюс движок
`engine/document.dart`. **Язык предикатов один на оба уровня** (§2 НОРМЫ):
`detect` вида документа и `detect` секции-маппера читаются одними
функциями — второго, «документного» языка норма не допускает, с ним
сниффер вернулся бы в код под другим именем.

Снято рукописного:

- порядок веток `body_decoder` (`vpn://` → эвристика base64 → `{`/`[` →
  `[Interface]` → строки) и `_detectFlavor` с `_looksLikeSingboxOutbounds`
  — уехали в данные буква в букву;
- `isDirectLink` больше не держит **вторую копию списка схем**: читает
  `kPipelineSchemes`. Копия успела разъехаться — `naive+quic://` конвейер
  разбирал, а классификатор ввода не знал, и вставка такой ссылки падала на
  «not a subscription URL, proxy link, or JSON». Тем же дефектом раньше
  были `socks4://` (§475) и `naive+https://`/`masque://` (§268), и каждый
  чинился правкой этой копии — по одной схеме за находку;
- `isWireGuardConfig` и `isAmneziaVpnLink` спрашивают реестр.

Оболочки остались КОДОМ (`qCompress`+zlib предикатами не выражаются), но
вызываются по ИМЕНИ из `unwrap` — так же у лаунчера. Вложенная оболочка
снимается с потолком `max_unwrap_depth: 2`.

Линтер `document_registry_test.dart`: каждый документ опознаётся
однозначно (победитель по `priority` один) и реестр даёт **ту же форму**,
что прежний порядок — ответ старого пути снимается до загрузки реестра,
то есть тем же кодом.

### Секции `singbox`

13 секций **порождены механически** (норма `MAPPER_ENGINE.md` §8a, ответ
лаунчера на находку 7: «да, генерируйте»); `trojan` и `vless` приехали
реестром 1.1.15. Пустая `params` — нормальное конечное состояние, а не
заготовка: вход уже в каноне ядра, переводить нечего. Диалект уровней у
`wireguard`/`tailscale` — форма `endpoint` (по `peers[]`) объявлена ПЕРЕД
веткой `default`, иначе `default` перехватил бы элемент.

Одна правка в коде: секция под FROZEN-ключом `mappers.<вид>` исполняема
**по определению**. Прежняя проверка «есть `params` с `source`» считала
пустую `singbox`-секцию отсутствующей, и элемент не достался бы никому.

**ГРАНИЦА соблюдена:** `parseSingboxEntry` и форма хранения `rawSource`
(§454–§456) не тронуты ни строкой — секция `singbox` работает ПЕРЕД
санитайзером, как нормализатор диалекта.

### Примитивы, добавленные волной

Все — общие, ни одного имени схемы в пакете движка.

| Примитив | Зачем |
|---|---|
| `json.type_of: {<путь>: object\|array\|string\|number}` | ФОРМА значения по пути: мусорный ТИП поля делает элемент нечитаемым целиком, и это не «поля нет» |
| `json.array_elem_any_keys` с несколькими `[]` | «хотя бы у одного элемента» по вложенным массивам: `[].outbounds[].protocol`. Рукописный сниффер смотрел ПЕРВЫЙ элемент и решал за весь массив |
| `json.any` / `all` / `not` | комбинаторы, которых у объектного предиката не было; у текстового они уже есть — выражение обязано читаться одинаково на обоих уровнях |
| `text.prefix_trim`, `text.min_len` | `{`/`[` после отступа; порог длины отсекает случайный текст, проходящий алфавит base64 |
| `ini.first_section_fold` | имя первой НЕ-КОММЕНТАРНОЙ секции INI (комментарий над `[Interface]` законен и несёт имя узла, G7) |
| `when: {lt, gt}` | числовое сравнение: диалект, где ЗНАК несёт смысл («любое отрицательное = выключено совсем»), набором значений не выразить |
| `type: duration_seconds` | длительность целыми секундами числом → строка с единицей |
| `default_from: "body.<путь>"` | дефолт из УЖЕ ПОСТРОЕННОГО тела: у объектного входа адрес лежит под якорем формы, и общий блок его пути не знает |
| `on_len_gt: {n, action, code}` | у источника-МАССИВА больше `n` элементов; сегодня лишние теряются молча (Q133-18) |
| `on_no_match: {action: drop_node}` | значение не попало ни в один ключ `sets` — у селектора рода узла это «узла нет» |
| `unknown_key.ignore` | ключи УРОВНЯ ДОКУМЕНТА, полем узла не являющиеся (`protocol`, `tag`, `remarks`) |
| `label.fallback.scheme` | историческое написание тега-фолбэка объявлено ДАННЫМИ, а не остаётся свойством кода (тег и есть identity) |
| `default_when: {absent: true}` у селектора | «не сказано» ЕСТЬ значение, и запись исполняется как обычная |

Две правки формы, сделанные по ходу: ключи общего блока в плоском наборе
получили ИМЯ БЛОКА (`tls.security`) — иначе одноимённая запись схемы
затирала селектор блока и TLS-блок у такой схемы не появлялся вовсе;
переопределением при этом считается совпадение имени **и источника**, а
одно имя над разными источниками — две разные записи.

### Что осталось

- **Диалекты форков в `params` секций `singbox`** (`$masque_flat`,
  `obfs` с `coerce.object_to_scalar`, `xmux.*` тремя написаниями,
  `$addressless`, `$credential` — список `SCHEMES.md` §13.3). Сегодня их
  исполняет `parseSingboxEntry`, и перенос обязан идти вместе со снятием
  соответствующих веток оттуда, а это правка ЧИТАТЕЛЯ ТЕЛА — за границей
  волны.
- ~~**`elements` реестра видов документа читает только линтер**~~ — закрыто,
  раздел 7d.
- ~~**Трасса** на объектном входе не снимается~~ — закрыто, раздел 7d.

## 7b. Что вышло: W7 (сборка ссылки от таблицы)

Направление второе, **таблица одна**. Эмиттер (`engine/emitter.dart`) обходит
те же записи секции задом наперёд: `maps_to⁻¹`, `value_map⁻¹`, `sets⁻¹`/
`implies⁻¹`, `scheme_sets⁻¹` (через `form_from`), userinfo `into⁻¹`/
`single_into⁻¹`, `compose` вместо `extract`. Имени схемы в пакете по-прежнему
нет: вход эмиттера — каноническое ТЕЛО плюс метка, а не модель `NodeSpec`.

**Переехали все 13 ссылочных схем.** Рукописных `toUri*` не осталось: в
`node_spec_emit.dart` живёт один `toUriTailscale` — у схемы нет URI-формы
вовсе. Вместе с ними удалены осиротевшие помощники (`_buildUri`, `_wrapIpv6`,
три `_vmess*FromTransport`, `serializeNaiveExtraHeaders`);
`transportToQuery` из `lib` больше не звался и **снят волной W8** — вместе с
ним переписаны на настоящий путь семь его вызовов в тестах `xhttp` и два в
`ws_early_data`: круг транспорта проверяется теперь ссылкой узла-носителя
(`toUri()` → `parseUri`), а не отдельной эмиссией, которой у транспорта не
бывает. Заодно видно и смену вида ссылки: `ed` уезжает отдельным параметром,
а не хвостом внутри `path` (обе формы читаются, пишется объявленная).

### Круг и чтение старых ссылок

Два стража, оба по фикстурам всех схем:

- **круг** `parse(emit(body))` — тело БАЙТ В БАЙТ, включая порядок ключей,
  плюс тег и identity (`engine_emit_roundtrip_test.dart`);
- **старые ссылки** — снимок `emit_before480.json` снят прогоном РУКОПИСНОГО
  эмита до его удаления. Эталон там — тело, в которое читалась САМА ССЫЛКА, а
  не тело исходного узла: между ними лежит потеря рукописного эмита, и
  требовать от новой волны восстановить то, чего в тексте ссылки нет, значило
  бы требовать невозможного.

**Починены потери, которых раньше не замечали:** весь корпус hysteria2 теперь
переживает круг (прежде семь кейсов из 23 исключались — `mport` и `pinSHA256`
не писались вовсе), `plugin` shadowsocks и `disable_sni` tuic уезжают в ссылку,
заголовки собираются обращением `extract`, и годность пары судит ТА ЖЕ
регулярка, что её читает (рукописная проверка имени заголовка удалена как
второй источник правды).

### Вид ссылки: было → стало

Текст ссылки волна меняет намеренно; каждое отличие — класс, а не случай.
Сверка по снимку «до», доля совпавших байт в байт:

| Схема | Совпало | Класс отличия |
|---|---|---|
| wireguard/awg, socks, ssh, masque, naive, shadowsocks | все | — |
| hysteria2 | 27 из 28 | пустой `@` у узла без пароля не пишется (форма, не значение) |
| http | 11 из 12 | `allowInsecure` → `insecure` |
| tuic | 11 из 16 | `allow_insecure` → `insecure` |
| trojan, vless, anytls | частично | `security=tls` не пишется (объявлено `omit_default` секции); `allowInsecure`/`allow_insecure` → единое `insecure` |
| vmess | 0 из 19 | порядок ключей контейнера и состав: пустые ключи (`aid`, `type`, `host`, `path`, `tls`) больше не пишутся, если тело их не несёт |

Все отличия — написание, а не смысл: тело на круге совпадает у всех кейсов.

### Атрибуты эмита: что уже в схеме реестра

`registry_mapper.schema.json` знает пять, и все пять исполняются как
написаны: `emit.form`, `emit.form_from`, `emit.param_order`,
`emit.omit_default`, `emit.emit_when`. `param_order` у нас принимает и
правило-строку `"alphabetical"` (так его пишут сегодняшние секции), и
перечень — схема объявляет массив, данные объявляют строку, и эмиттер
исполняет обе формы.

### Расширения грамматики эмита — СВЕДЕНЫ (GRAMMAR_SYNC «Сведение №2», §5)

Заводились по правилу «сперва ищи аналог»: всё, что выражалось существующим
примитивом, новым именем не называлось. Имена читаются в ОДНОМ месте —
`EmitNames` в `engine/emitter.dart`, по образцу `DraftNames`. Расчёт оправдался:
по итогам сведения префикс снят у четырёх имён правкой одной строки каждое.

К правилам выбора имени лаунчер добавил **пятое, специфичное для эмита**:
*ссылка, которую мы пишем сегодня, не меняется без строки в `DELTAS.md`* —
`rawSource` ручного узла перезаписывается при пересохранении, и смена написания
молча переписывает то, что человек когда-то вставил сам. Единственное принятое
исключение — `%20` вместо `+`.

| Имя (итоговое) | Зачем | Итог сведения |
|---|---|---|
| `round_trip: false` + `round_trip_why` | объявленный отказ от обратного хода. Без него потеря на круге молчалива; объявленная — видна линтеру и отчёту, а не телу узла пользователя | **принято как есть** |
| `emit_as` (`join` / `bool01` / `json` / `raw`) | как сериализуется значение, когда тело хранит его не строкой. `alpn` в теле список, в ссылке строка через запятую; `insecure` в теле булев, в ссылке `1`. Угадывать по типу нельзя: булев, писанный `true`, и булев, писанный `1`, — разные ссылки у живых панелей | **принято как есть**; префикс `emit_` у атрибутов ЗАПИСИ сохраняется — он отличает атрибут выхода от атрибута входа в одном словаре |
| `compose` строкой ЛИБО `{template, from, omit_when_empty}` | обращение `extract`: один параметр из нескольких путей тела. Регуляркой обратный ход не выражается — шаблон объявляет его прямо | **сведено к FROZEN-имени**: `compose` в схеме уже был строкой, добавлена объектная форма (расширение значений, не новое имя) |
| `emit.omit_port` | порт, опускаемый в ссылке. Обращать `defaults.server_port` напрямую нельзя: у схемы бывает дефолт РАЗБОРА без права опускать порт на выходе — иначе ссылка перестала бы читаться клиентами, которые дефолта не знают | **принято, префикс снят** (внутри `emit` он называл бы `emit` дважды). У нас выставлен, у лаунчера объявлен и не выставлен: его `hostPort` пишет порт всегда — расхождение объявлено, выравнивание отдельной дельтой |
| `emit.userinfo`: строка либо `{form, padding}` | выходная форма userinfo. `userinfo.decode` разбора — конвейер ПОПЫТОК (`percent`, `base64?`), и обратить его нельзя: `base64?` значит «может быть, а может и нет» | **принято, префикс снят**; паддинг вынесен третьим ключом — наш base64 без `=`, у лаунчера с `=`, обе стороны читают обе формы, и ни одна не меняет свою молча |
| `emit.json_map` + `emit.json_always` | форма, собирающая не query-ссылку, а base64(JSON) | **принято, префикс снят.** У нас `json_map` вдобавок НЕ ОБЪЯВЛЯЕТСЯ, а выводится из источников записей той же формы: объявленная отдельно, она была бы вторым источником правды |
| ~~`emit_name`~~ | написание имени в ссылке, не равное канону разбора | **ОТКЛОНЕНО.** Норма: каноническое имя на выходе — первое в `aliases`, без исключений; расхождение чинится перестановкой `aliases` плюс строкой в `DELTAS.md`. Отдельный атрибут позволял бы входу и выходу разъехаться молча |

**Перестановок `aliases` нам не потребовалось ни одной**: у записей,
где выход исторически расходился с каноном, ключ реестра уже совпадает с тем,
что мы пишем.

## 7c. Что вышло: W8 (вид ссылки = де-факто формат схемы)

Вид ссылки волна W7 меняла намеренно, и таблица выше это фиксировала. Ревизия
показала, что часть отличий **ломает совместимость**, а не только написание:
`toUri()` у нас одновременно форма хранения ручного узла (`rawSource`) и то,
что уезжает по кнопке **Copy link** в v2rayN, v2rayNG, NekoBox, Hiddify и
Shadowrocket. Круг у чужого клиента свой. Опущенный `security=tls` круг
проходит — наш разбор поднимет TLS умолчанием, — а у Xray-клиентов умолчание
`security` для vless равно `none`, и тот же узел встаёт у соседа БЕЗ
шифрования. Написание `insecure` вместо `allowInsecure` читают не все.

Отсюда уточнение нормы, принятое владельцем и лаунчером: **вид ссылки = де-факто
формат схемы**, а не канон нашей таблицы. Из него следует отмена решения
W7 по `emit_name`.

### `emit.names` — принято вместо отклонённого `emit_name`

Отклонение держалось на доводе «выход и вход разъедутся молча». Довод снимается
ограничением набора, а не отказом от имени: **выходное написание выбирается ИЗ
написаний самой записи** (имя, алиасы, query-источники), и это судит линтер
секций — запись, которая пишет то, чего не читает, отдаёт ссылку, которую мы
сами не разберём. Имя читается в `EmitNames`, набор написаний даёт общая
`readableNames` — одно правило, одно место, общее у эмиттера и линтера.

Объявляется оно **у СХЕМЫ**, а не перестановкой `aliases` общего блока, и это
существо решения. Признак «не проверять сертификат» читается всеми написаниями
сразу, но ПИШЕТСЯ у каждой схемы своим — тем, которое читают её клиенты:
`allowInsecure` у vless/trojan/anytls/http, `allow_insecure` у tuic, `insecure`
у hysteria2 (официальная спека). Перестановка `aliases` блока `tls#uri`
навязала бы одно написание всем схемам разом.

### Страж вида ссылки

`engine_emit_shape_test.dart`: **ссылка = снимок `emit_before480.json` байт в
байт**, по всем 13 схемам, кроме ЗАКРЫТОГО поимённого списка отличий с
причиной. Классом («у vless теперь иначе») отличие не объявляется — классом
прикрывается и то, чего не заметили. Список судится в обе стороны: разрешённый
кейс, у которого отличия больше нет, тоже валит тест, иначе строка прикрывала бы
будущую поломку.

Осталось разрешённых отличий девять кейсов плюс один класс, и все они —
**починки потерь**: поле, которое тело несёт, а рукописный эмит не писал
(`transport.path` и `transport.mode` у vless, `transport.host` у контейнера
vmess, `disable_sni` с `sni` у tuic), либо написание схемы `awg://` у узла с
AWG-полями (обращение `kind_when`, объявлено разделом 7b).

### Данные, которыми секции сведены к снимку

| Схема | Что объявлено | Почему |
|---|---|---|
| vless | `names: insecure→allowInsecure`; `security` вычеркнут из `omit_default`, объявлен `emit_when: always` | умолчание `security` у Xray-клиентов — `none`: ссылка без параметра читается как узел без TLS |
| trojan | то же, `omit_default` пуст | так писал рукописный эмит |
| anytls | то же; ветки `sets` записи `security` переставлены, ветка `reality` возвращена | запись перекрывает одноимённую из `tls#uri` целиком и вместе с ненужной ей веткой `none` теряла нужную `reality`; порядок объявления и есть выбор между ветками, которые тело подтверждает одинаково |
| tuic | `names: insecure→allow_insecure` | де-факто написание диалекта |
| http | `names: insecure→allowInsecure`; черновик стал оверлеем | полная копия секции устарела с приходом зеркала и всё равно проигрывала реестру — её правки не исполнялись вовсе |
| hysteria2 | `userinfo.empty_separator: true` | `@` у узла без пароля — часть сегодняшнего вида ссылки; из разбора флаг не выводится (пустой и отсутствующий userinfo неразличимы) |
| vmess | `param_order` перечнем (порядок ключей контейнера); `json_always` картой; `names: serviceName→path` | контейнер читается частью клиентов позиционно; `aid` пустой строкой читается хуже нуля, `type: none` и `net: tcp` — значения, а не пустота; имя gRPC-канала v2rayN кладёт в `path` |
| transports (xhttp) | `emit_as: raw` у трёх булевых | написание булева (`true` против `1`) — свойство параметра, а не типа значения |

### Правки движка

Четыре, все — правила, а не случаи:

1. **`emit.names`** плюс `readableNames`: выбор написания из тех, что запись
   читает.
2. **`emit_when: always` сильнее ветки умолчания.** Прежде умолчание
   побеждало равную по весу ветку и возвращало `null` до проверки
   `emit_when` — «разбор восстановит сам» верно для НАШЕГО разбора, и у
   чужого клиента умолчание бывает другим.
3. **Запись-селектор с явным `maps_to: null` остаётся селектором.** Её
   обратный ход — ветка `sets`, а не `maps_to`; так написан отказ от общей
   записи блока, и вырезать её значило потерять параметр, который тело
   несёт ветками.
4. **Ветка `sets` не пишется, когда все её пути принадлежат ДРУГИМ записям
   напрямую.** У такого пути есть хозяин — запись с `maps_to` в него, — и на
   чтении побеждает он (priority). Обратный ход повторяет тот же выбор:
   иначе составное имя ветки уезжало вместо прямого параметра, и клиент читал
   поле, которого автор не писал.

`param_order` теперь задаёт порядок и у формы-контейнера: правило одно, форм
две. `json_always` принимает карту «ключ → заполнитель» вдобавок к перечню.

### Нормы, исполненные как написаны

- **порядок параметров** — алфавитный;
- **пробел на выходе** — `%20`, не `+`;
- **литеральный `+` в значении** — `%2B`: чтение декодирует `+` как пробел
  (form-encoding), и без этого base64-ключ вернулся бы с пробелом — тот самый
  класс дефектов, который W4 чинила на входе (D133-7);
- **каноническое имя параметра** — первое в `aliases` (§0.6);
- **IPv6** — в скобках.

### Обращение `value_map`

Канон написания объявлен ПОРЯДКОМ — тем же правилом, что и каноническое имя
параметра:

- **неинъективная таблица — норма, а не ошибка.** В одно значение тела ведут
  несколько написаний (`on`, `true`, `1` — всё это «включено»); обратный ход
  берёт ПЕРВОЕ ОБЪЯВЛЕННОЕ. Прежняя редакция этого раздела требовала
  инъективности и отказывала — отказ ронял вид ссылки AWG3;
- ветка со значением `null` (`fp: {random: null}`) из обращения выпадает: она
  означает «поле не ставится», а не «значение такое»;
- **канон, которого таблица не переводит, не подменяется.** Таблица переводит
  алиасы в канон и тождественной пары не содержит: канон стоит справа, слева
  его нет. Обрати её вслепую — канон уехал бы под именем алиаса, а когда алиас
  вдобавок нагружен своим `sets`, узел на обратном чтении получил бы поле,
  которого у него не было.

### Ещё три обращения, которых потребовали схемы волны

- **`form_from: {any_set: …}`** — обращение `kind_when`. Род узла у части схем
  объявлен НАБОРОМ полей, а не значением одного пути, и написание схемы обязано
  его нести: узел с AWG-полями, уехавший как `wireguard://`, теряет род,
  который вход объявил схемой. Имя предиката взято у `kind_when` разбора —
  тот же предикат в ту же сторону, второго словаря не заводится.
- **умолчание побеждает равную по весу ветку `sets`.** Тело, одинаково
  объяснимое и умолчанием, и явным значением, писать явным не нужно: разбор
  восстановит его сам. Этим снят лишний `security=tls` у схем, где шифрование
  несёт само написание.
- **userinfo, названный ПЕРВЫМ источником обычной записи.** У туннельных схем
  блок `userinfo` секции пуст, а канон объявлен порядком источников записи
  (`["userinfo", "query.<имя>"]`): не напиши мы канон, ключ уезжал бы
  параметром — ссылка осталась бы читаемой, но сменила бы вид у всех живых
  узлов схемы.

### Что осталось нерешённым: род узла без единого своего поля

Узел, у которого вход просил AmneziaWG, но НИ ОДНО поле не уцелело (`jc=abc`
вместе с одиноким `jmin`), от обычного WireGuard неотличим: рода нет ни в
каноническом теле, ни в модели `NodeSpec` — он восстанавливается перепарсом
`rawSource`. Эмиттер такой узел отдаёт написанием `wireguard://`.

Сегодня это безвредно и проверено кругом: потолок MTU уже применён и уезжает в
ссылку числом (`mtu=1280`), identity и тег не двигаются. Станет вредно, когда
правило реестра перейдёт с `any_set` по телу на `when.source_kind` — тогда роду
нужно место в модели (поле рода у `WireguardSpec` либо признак в
`extensionFields`). **Это решение по МОДЕЛИ, а не по грамматике эмита**, и
принимается оно владельцем вместе с переходом правила.

## 7c. Сведение грамматики (GRAMMAR_SYNC 5d8adc80)

Лаунчер свёл грамматику двух сторон по каждому примитиву, который эта фича
завела: `SPECS/133-F-N-REGISTRY_DRIVEN_LINK_MAPPER/GRAMMAR_SYNC.md`, норма —
`PRIMITIVES.md` §0.11, ответ нам — `contract/TASKS_LXBOX.md` §24.27. Правило
выбора имени: аналога у них нет — берут наше; аналог есть под их FROZEN-именем
и исполняется — остаётся их; новый примитив дублирует существующий — сводится
к существующему. Переименований FROZEN-атрибутов нет ни у одной стороны.

**Шесть наших имён приняты добавлением** (`text.prefix_trim`, `text.min_len`,
`ini.first_section_fold`, вложенные `[]` у `json.array_elem_any_keys`,
`when{lt,gt}`, `unknown_key.ignore`), **шесть совпали независимо**
(`json.type_of`, `on_len_gt`, `on_no_match`+`drop_node`,
`default_when{absent}`, `$host`, `on_item_invalid`), **четыре сведены к
именам лаунчера** — их и правит эта волна:

| Было у нас | Стало | Причина отклонения |
|---|---|---|
| `json.any/all/not` внутри `json` | композиция уровня `detect` | дубль: иначе `json`/`ini`/`text` заводят по своей копии трёх операторов |
| `type: duration_seconds` | `normalize: duration_bare_seconds` | дубль: разница только в месте объявления, а две дороги к одному результату расходятся на первой же схеме |
| `label.fallback.scheme` | `label.fallback.scheme_source` | дубль: «объявить написание данными» и есть `scheme_source: as_written` |
| неявное «одинокий userinfo → первое имя `into`» | явный `single_into` | неявность даёт неверный ответ там, где конвенция другая: у `naive` одиночный userinfo — ПАРОЛЬ, то есть второе имя |

Вид источника (`source_kinds.json`, до переименования — `documents.json`)
лаунчер **принял за основу** и вернул
шесть правок: имена на FROZEN (`id`→`kind`, `has_key`→`required_keys`,
`reunwrap`→`redetect`, `element_kind`→`mapper`, `yields`→`elements`,
`amnezia_vpn_link`→`amnezia_vpn`), `json.not` → композиция `detect`, снятие
`contains "[Peer]"` у `wireguard_conf` (**правда наша**: у заготовки без пира
`[Peer]` отсутствует законно), удаление ветки `clash_yaml`, `desc_en`/`desc_ru`
всем веткам.

### Что удержано оверлеем, а не переименовано

- **`defaults.server_port` у xray-секций — расхождение сторон, не дефект.**
  У лаунчера дефолта нет и элемент без порта отбраковывается целиком; у нас
  даёт узел, как было. Это состав подписки, а не грамматика, и молча принять
  чужое поведение не может ни одна сторона. Оверлей помечен полем
  `_divergence` со ссылкой на §24.27 и снимается только решением владельца.
- **Умолчание «первое имя `into`» в движке СНЯТО БЫТЬ НЕ МОГЛО.** Черновики
  волны объявляют `single_into` явно, но живой реестр (`assets/contract`,
  зеркало лаунчера) проставляет его только там, где поле не первое, — у
  четырёх секций (`shadowsocks`, `socks`, `ssh`, `vmess`) двухимённый `into`
  полагается ровно на это умолчание. Снятие обнулило бы их первое поле,
  то есть identity живых узлов, правкой в чужом зеркале. Умолчание уходит
  вместе с синком реестра, где `single_into` проставлен всем; судья —
  `before_480_identity_snapshot_test.dart`, который этот откат и поймал.
- **`alpn` с Xray-входа** остаётся `maps_to: null` у обеих сторон: лаунчер
  подтвердил, что ALPN оттуда не читается ни общей веткой TLS, ни REALITY
  (исключение — hysteria, где чтение уже есть). Включение — отдельной дельтой.
- **Ветка `clash_yaml` убрана из реестра, но форма ответа сохранена.**
  У лаунчера её заменяет `on_unrecognized`, который наш движок ещё не
  исполняет; чтобы «ноль узлов» не подменился на «список ссылок из одной
  строки JSON», опознаваемый JSON без своей ветки разбирается прежним
  рукописным порядком. Ветка вернётся снятием заплаты, когда
  `on_unrecognized` заработает.

### Сведение №2 (GRAMMAR_SYNC 8602a857, §5–§9 и PRIMITIVES §0.13)

Вторая волна свела примитивы нашей W4. Норма §6.0 («маппер переносит,
санитайзер судит; исключение — структурный отказ») принята: проверка при
заведении правила — «если это убрать, тело построится?». Построится —
правило принадлежит `body.fields`.

- **`range_order` в маппере — отклонён как дубль, и это верно.** Правило
  судит ЗНАЧЕНИЕ, тело строится и без него, а у нас `normalize: range_order`
  уже живёт в `body.fields` реестра. Снятие из секций — за агентом
  wireguard/INI, чей файл это (`uri/wireguard.json`, 10 записей
  `range_order:swap|strict`); судья — его же кейсы W4.
- **`cidr_prefix` и `base64_std` остаются пока в маппере** с пометкой
  `_awaitingContractSync`: лаунчер переносит их в `body.fields` отдельным
  контрактом, и до его прихода снятие оставило бы поля без нормализации
  вовсе. У `base64_std` перенос вдобавок двигает identity живых узлов
  (канонизация ключа меняет тело) — обязательна строка `DELTAS.md`.
- **`prepend_group` — живой случай у нас ЕСТЬ**, имя закрепляет не
  частность: секция `uri/hysteria2.json`, запись `$multiport`, член
  `into.spec` (`prepend_group: "first"`). Одно значение источника
  (`port_raw`) читается ДВАЖДЫ в разной нарезке: первый порт едет числом в
  `server_port`, а вся спецификация вместе с ним — списком диапазонов в
  `server_ports`. Пример-ссылка — фикстура
  `hysteria2/mport_authority_comma_recovery`:
  `hysteria2://pass123@example-1.com:443,20000-30000/?insecure=1#hop`.
  `merge: prepend` его не заменяет: тот работает на одном пути, а здесь
  склеиваются две группы ОДНОЙ регулярки до записи в тело.

#### Несогласие: `kind_when` к `when` + `sets` не сводится

Лаунчер отклонил `kind_when` как третью дорогу к результату, который даёт
присваивание селектора. **У нас это не так, и кейсы корпуса это показывают.**

`sets` пишет ПУТИ ТЕЛА (`_applySets` → `_write`). `kind_when` не пишет тело
вовсе: он отвечает именем РОДА, которое читается снаружи маппера
(`uri_pipeline.dart:368`) и включает потолок MTU с кодом
`awg_mtu_clamped` из реестра. Ни рода, ни кода `sets` произвести не может.

Подменить его правилом реестра тоже нельзя, и по той же причине, по которой
`kind_when` заведён: `body.fields.mtu.max_when.when.any_set` судит ТЕЛО, а
у двух кейсов корпуса в теле не осталось ни одного AWG-поля —
`awg_bad_numeric_skipped` (`jc=not-a-number` + одинокий `jmin`) и
`awg_jc_invalid_dropped` (`jc=abc` + `jmin=50`). Оба значения негодны и
сняты общим правилом поля, тело такого узла от обычного WireGuard
неотличимо, и снимок ждёт у обоих `mtu: 1280`. Условие обязано смотреть на
ВХОД (`query.jc`), а не на тело: узел ПОПРОСИЛ AmneziaWG, и потолок — это
свойство запрошенного протокола, без которого туннель поднимается, но
данные по нему не идут.

Вывод для сведения: `kind_when` и `when`+`sets` отвечают на разные вопросы —
«каким родом объявил себя ВХОД» против «что записать в тело». Сводить их
можно будет, когда у присваиваний появится адресат вне тела; сегодня
отклонение снесло бы два зелёных кейса снимка. Переформулировка не сделана
намеренно — отчёт с фактами ушёл лаунчеру.

## 7d. Что вышло: INI и обход элементов

Два блока в `develop`: вход `.conf` на движке и обход элементов документа
от реестра. Оба закрывают долги раздела «Что осталось» волны W5–W6.

### INI/`.conf` на движке

**Третий вход движка** рядом с лексером ссылки и объектным входом:
`engine/ini_space.dart` раскладывает текст в пространство `ini.<Section>.<Key>`
по диалекту, который объявила САМА СЕКЦИЯ (`ini_dialect`, §0.11 DRAFT), а не
по правилам, зашитым в адаптер. «Как принято в wg-quick» — это один диалект
из нескольких возможных, и второй (панельный `.ini`, `.ovpn`) потребовал бы
ветки в коде, будь первый умолчанием движка.

`mappers/wireguard_mapper.dart` **удалён целиком**: `mapWireguardIni`,
`_splitEndpoint`, `_mapWireguardParams`, `_cidrList`, `_reserved`. Из
`ini_parser.dart` снят `peerCommentName` — цепочка имени (комментарий под
`[Peer]` → подсказка вызывающего → `WireGuard`) объявлена `label.source`
секции. Тег И ЕСТЬ identity, и порядок звеньев обязан быть данными.

Примитивы — все общие, ни одного имени схемы в пакете движка:

| Примитив | Зачем |
|---|---|
| `detect.ini` (`sections` / `keys_any` / `keys_all`) | опознание формы по РАЗОБРАННОМУ пространству; тот же язык предикатов, что у текста и объекта (§2 НОРМЫ) |
| `on_no_match: {action: take_all, into, defaults}` у `extract` | регулярка не разложила значение, и объявленный ответ «взять целиком», а не потерять. G7: голый IPv6 в `Endpoint` (§219) |
| источник `hint` | имя, предложенное вызывающим: в документе его нет, но МЕСТО в цепочке метки остаётся данными |
| `ini_dialect` | `line_comment_prefixes` по GRAMMAR_SYNC §0.11; прежнее `comment_prefixes` читается тоже — секция приедет реестром с новым именем, а черновик несёт старое, и разъехаться они не должны молча |

Две правки формы по ходу: звено цепочки метки считается ответившим по
ПОСЛЕ-нормализационному значению (пробельная подсказка больше не занимает
место в цепочке и не глушит следующие звенья); `withParams` перестал терять
`kind_when` при вмонтировании `include` — до правки род узла, объявленный
входом, пропадал у любой секции с `include`.

**Дельт нет.** 27 INI-кейсов снимка `wireguard/pipeline_identity_before.json`
сходятся байт в байт, включая голый IPv6 адресом целиком, чтение только
первой `[Peer]` и тишину `Interface.DNS`.

Коды `wgconf_dns_ignored` и `wgconf_extra_peer_dropped` объявлены секцией
ВПЕРЁД своего текста: в `warnings.json` контракта их нет. Ставить их значило
бы показать человеку предупреждение с ПУСТЫМ текстом там, где сегодня
законная тишина, а дельты на это нет. Имена лежат под `$code_pending` —
вернуть их будет правкой одного ключа, когда текст приедет синком. Сами
записи при этом обязаны остаться: без записи `dns` уехал бы в
`uri_param_unknown`.

### Обход элементов документа

`elements` реестра читал только линтер: опознание ДОКУМЕНТА было объявлено
данными волной W6, а путь к его ЭЛЕМЕНТАМ оставался `switch (flavor)` в
`parse_all` — второй копией того же знания. Теперь путь исполняет движок
(`DocumentRegistry.groupsFor`), а ветка документа едет в `JsonConfig.source`
целиком.

Снято рукописного: четыре ветки `switch (j.flavor)` для sing-box — «массив
конфигов», «массив outbound'ов», «одиночный», «полный конфиг». Это были
четыре способа добраться до элементов ОДНОГО диалекта, и все четыре
выражаются строкой `elements`. Сборка документа Xray (порядок §321, дедуп
§404, владение именем §342) вынесена в `_parseXrayDocument` без правок:
способ ДОБРАТЬСЯ до элементов называет реестр, а что с ними делать
дальше — принадлежит сборке документа и остаётся кодом.

Результат обхода — ГРУППЫ, а не плоский список: границы конфига несут смысл
для дедупа и владения именем, и схлопни обход их в один список, узлы разных
конфигов начали бы вытеснять друг друга.

**`JsonFlavor` НЕ снят намеренно.** Его читают превью вставки из буфера
(`clipboard_analysis.dart`) и контроллер подписок, то есть UI, а UI эта
задача не трогает. Он остался публичной формой и запасным путём для
документа, опознанного БЕЗ реестра: опознание документа, в отличие от
разбора узла, обязано работать и без реестра — на нём стоит вся вставка из
буфера, и молчаливый отказ выглядел бы как «подписка пустая».

> **СНЯТ** задачей [§483](../../tasks/483-remove-json-flavor.md)
> (19.09.2026): `JsonConfig` несёт ветку-источник обязательным полем, вид
> читается её ключом `kind`, и запасной путь отвечает той же веткой — то
> есть вторым обходом элементов быть перестал. Видимые строки превью не
> менялись.

### Трасса на объектном и текстовом входе

Первая строка трассы (`elem_detect` — какая секция и какая форма взяли
элемент) пишется в `execute()`, то есть в ОДНОМ месте на все три входа:
ссылка, объект и текст INI сходятся там, и строка «что опознано» обязана
быть у всех трёх одинаковой — иначе механическая сверка Go↔Dart ловила бы
ложное расхождение на самом первом событии. Прежде трасса снималась только
на ссылке, и вход, который её не пишет, из сверки просто выпадал.

### Что осталось после блока

- **Строка `elements` разбирается по форме, а не по полному пути.**
  `[].outbounds[]` и `outbounds[]+endpoints[]` дают одинаковые группы:
  ХВОСТ пути проходит сборка документа (`parseSingboxConfigs` сама читает
  `outbounds` и `endpoints`). Пока обе стороны пишут ровно эти две формы,
  разницы нет; третья форма пути потребует разбора хвоста движком.
- **Диалекты форков в `params` секций `singbox`** — как было, за границей
  волны (правка читателя тела).

## 8. Риски

- **Секция реестра пишется у лаунчера по Go-поведению.** Там, где Dart
  сегодня ведёт себя иначе, выбирает `DELTAS.md`, а не молчаливый перенос.
  Фикстуры W0 — единственное, что отличает «починили» от «сломали».
- **`toUri()` — форма хранения ручного узла.** Смена вида ссылки перезапишет
  `rawSource` при пересохранении. Identity (тег) не двигается; критерий 5.2
  обязателен до W7.
- **Реестр становится обязательным в рантайме разбора.** Сегодня часть путей
  работает и без него (`kAwgMtuFallback`); после W8 — нет.

## 8. Дельты волны W4 (hysteria2 / tuic / masque / wireguard + INI)

Оформление — пометка `delta480` у кейса фикстуры, «было → стало» ниже.
Всё, что в этот список не попало, — не дельта, а баг секции.

| # | Схема | Кейс фикстуры | Было → стало | Основание |
|---|---|---|---|---|
| delta480-1 | hysteria2 | `b480:pinsha256_pair` | `pinSHA256=a,b` давал ПУСТО (значение с запятой не читалось) → список `["a","b"]` | D133-11 |
| delta480-2 | masque | `b480:publickey_raw_plus` | сырой `+` в `publickey` становился пробелом и ключ терял первый символ → `+` читается буквально | D133-7 |
| delta480-3 | wireguard | `b480:publickey_raw_plus`, `b480:privatekey_raw_plus_query`, `b480:presharedkey_raw_plus` | сырой `+` в ключе ронял УЗЕЛ целиком (`body: null`) → узел живёт, ключ читается буквально | D133-7 |
| delta480-4 | wireguard INI | `ini:b480_preshared_key_snake` | `preshared_key` в `[Peer]` молча терялся (читался только `PresharedKey`) → алиас читается | «чиним молча теряемое» |
| delta480-5 | все четыре | имена параметров | `SNI=`, `AllowInsecure=` и прочий регистр не читались → общий fold-case | D133-9 |
| delta480-6 | hysteria2/tuic/masque | `insecure` | 4 написания → 9 из `tls.json` | D133-10 |
| delta480-7 | tuic | кейса в фикстуре нет (см. ниже) | пустой пароль (`uuid:@host` и `uuid@host`) молчал → узел получает код `password_empty` на пути `password` | решение владельца 19.09.2026 |
| delta480-9 | hysteria2 | `uri/hysteria2/mport_host_port_item_dropped` (корпус) | негодный элемент `server_ports` терялся МОЛЧА (наша починка 44f26d11 на маппере) → элемент выбрасывается с кодом `hysteria2_server_ports_item_invalid` на пути `server_ports[0]`, и правило действует на всех входах, а не только на ссылке | контракт 1.1.40 §36.1 |

**delta480-7 — пустой пароль TUIC.** Лаунчер держал это открытым вопросом
(`TASKS_LXBOX` §32.3, `QUIRKS.md` Q133-67): «негодная ссылка или законный
узел?» — и кейса в корпус не заводил, потому что корпус нормирует
согласованное. Ответ владельца: **законный узел с предупреждением**.
Соединение возможно по устройству протокола — токен TUIC v5 это
TLS-экспортёр, пароль идёт КОНТЕКСТОМ, и пустой контекст экспортёр не
отвергает. Отбраковка выбрасывала бы живые узлы, молчание скрывало бы от
человека, что учётных данных у узла фактически нет.

Дельта односторонняя и **отбраковки не снимает — её уже не было**: синк
1.1.33 привёз секцию без `required: true` у `password`, и оба написания
разбирались в узел ещё до этой правки. Добавлен ровно код.

Форма тела не тронута: `password: ""` у обоих написаний, как и прежде. Ядро
читает отсутствующий ключ и пустую строку одинаково (`option/tuic.go`
`Password` с `omitempty`), а второе написание «нет пароля» двигало бы тела
живых узлов без нужды. Оба написания входа дают ОДНО тело — различать их
нечем и незачем.

Кейса в `pipeline_identity_before.json` нет и не заводится: снимок
нормирует identity, а identity узла с паролем не сдвинулась (проверено —
все кейсы фикстуры дают прежние хеши). Поведение держат тесты группы
«пустой пароль — узел с кодом, не отбраковка» в
`tuic_pipeline_invariants_test.dart`.

Код объявлен ОВЕРЛЕЕМ (`contract_draft/uri/tuic.json`) под
`$code_pending`/`_awaitingContractSync`: в `warnings.json` его ещё нет —
лаунчер заводит его ОБЩИМ (не под схему), `severity: warning`,
`params: [path]`. До синка узел получает код без текстов (заголовком встаёт
сам идентификатор, карточка пуста, severity по умолчанию и так `warning`);
с приходом версии тексты подхватываются сами, без правки кода, а оверлей
снимается — правило станет реестровым.

Примитив `on_empty: {code}` — ОБЩИЙ, не под схему: судит не написание
значения (это делает `on_invalid`), а его НАЛИЧИЕ, чего не ловит ни одна
проверка типа — пустая строка форму не нарушает. От `required` отличается
тем, что узел остаётся: «значения нет» — цена соединения, о которой
человеку говорят, а не приговор записи.

**Не дельты, воспроизводится как есть:**

- INI `Endpoint = 2001:db8::1:51820` — голый IPv6 берётся адресом ЦЕЛИКОМ,
  порт по умолчанию 51820 (§219: отличить порт от адреса нечем). Кейс
  `ini:b480_endpoint_bare_ipv6`; в `DELTAS.md` лаунчера этого нет, значит
  чинить не наша волна.
- INI: читается только ПЕРВАЯ `[Peer]`, прочие отбрасываются
  (`repeat: "first_only"`); код `wgconf_extra_peer_dropped` объявлен в
  секции, но сегодняшнюю тишину не отменяет.
- `Interface.DNS` — лоссы by design. Код `wgconf_dns_ignored` объявлен
  секцией, но НЕ ставится. Контракт 1.1.22 привёз обоим кодам ТЕКСТ, и этого
  всё ещё мало: корпус их не ждёт (`wgconf/ini_basic.body` требует пустой
  список warnings), а `desc` самих кодов говорит «сегодня Go делает это
  молча». Оба лежат под `$code_pending` и включатся вместе с правкой
  корпуса — см. раздел 9.

## 9. Что вышло: синк 1.1.22

Источник — `develop` лаунчера `4aeca7a7` («род узла в условии, cooccurrence,
cidr_prefix/base64_std в теле»), зеркало реестра 1.1.16 → **1.1.22**.

### Снято с нашей стороны

| Что | Чем заменено |
|---|---|
| Обход `kindIsAwg` снаружи маппера (`uri_pipeline.dart`) | `when.source_kind` правила реестра; род едет контекстом рядом с `body_source` |
| Поле `UriMapping.kindIsAwg` | `UriMapping.kinds` — имена ИЗ ДАННЫХ (`kind_when` секции), толкует их реестр |
| Варианты `base64_std` / `cidr_prefix` у маппера | те же нормализаторы в `body.fields` (D133-22): правило действует на ВСЕХ входах |
| Три записи `kNotModelled` (ключи WG) | канон ставит санитайзер, поле переживает круг как любое другое |
| Полные копии черновиков `uri/{tuic,hysteria2,masque}` | секции приехали в реестр исполняемыми; у tuic и hysteria2 остался оверлей на `emit` |

Запасное число `kAwgMtuFallback` осталось ровно в одном месте — когда реестр
НЕ ЗАГРУЖЕН и судить значение некому: AWG-узел без потолка поднимает туннель,
по которому не идут данные, и молчание там не безопасно.

### Новое в движке (примитивы контракта)

- **`scheme_sets` с ключом `"*"`** — присваивания, общие для всех написаний
  схемы. У QUIC-схем TLS включён всегда, параметра `security` в ссылке нет, и
  выразить это записью нечем. Общий набор кладётся ПЕРВЫМ, конкретное
  написание вправе его переопределить.
- **`params.<ключ>: null`** — ключ объявлен, но телом не становится. При
  `unknown_key.action: keep` отсутствующая запись дала бы повод коду
  `uri_param_unknown`, объявленная — нет: потеря осознанная.
- **Область `authority` раскрывается ДО лексера.** base64 законно несёт `?`
  и `/`, и лексер, увидев их в ещё закрытом тексте, отрезал бы по ним путь и
  query. Прежде шаг с областью исполнялся только после лексера, и форма, чья
  оболочка накрывает весь пэйлоад, теряла все параметры.
- **`when.source_kind`** в `max_when`/`min_when`/`default_when`, оператором
  рядом с `any_set`. Операторы соединяются **ИЛИ**: `source_kind` знает род от
  входа и работает там, где в теле не осталось ни одного опорного ключа;
  `any_set` судит тело и остаётся навсегда, потому что у входа в собственной
  форме ядра рода от входа нет вовсе.
- **`body.relations` kind `cooccurrence`** с оператором `$range_width` —
  цена СОЧЕТАНИЯ настроек, ни одна из которых не битая. Дедуп по `code`+путь.

### Расхождения, переданные лаунчеру

| Секция | Отличие | Почему |
|---|---|---|
| `uri/trojan`, `uri/vless` | оверлей добавляет `dialer#uri` к `include` | keep-alive параметрами ссылки (§453) читается у нас; у лаунчера эти поля, по-видимому, читает другой слой |
| `xray/trojan`, `xray/vless` | оверлей добавляет `dialer#xray` | то же самое на входе Xray |
| `conf/wireguard` | коды `wgconf_dns_ignored`, `wgconf_extra_peer_dropped` под `$code_pending` | ТЕКСТ кодов контракт 1.1.22 привёз, но КОРПУС их не ждёт: `wgconf/ini_basic.body` требует пустой список warnings, ни один кейс `contract/corpus/**` не несёт `wgconf_*`, а `desc` самих кодов говорит «сегодня Go делает это молча». Включить раньше корпуса значит разойтись с лаунчером на живых узлах |
| `conf/wireguard` | цепочка метки без звена «хост Endpoint» | безымянный `.conf` у нас даёт «WireGuard»; звено Endpoint сдвинуло бы identity живых узлов |
| xray-секции | `defaults.server_port: 443` | `_divergence`, решение владельца: у лаунчера элемент без порта отбраковывается целиком, наш дефолт создаёт узел там, где у него узла нет вовсе — это изменение СОСТАВА подписки |
| `uri/*` | плоский `?ed=` без `eh` | per-app |

**Оверлей `include` заменяет список реестра ЦЕЛИКОМ, а не дополняет его.**
Контракт 1.1.22 вынес `security` из `tls#uri` в отдельный блок
`tls#uri_security`, и пока списки оверлеев не выровняли, блок `tls` пропадал
у КАЖДОГО узла trojan и vless. При каждом синке списки сверять с реестром.

## 10. Что вышло: синк 1.1.26

Источник — `develop` лаунчера `a0c403ab` («dialer#uri у vless/trojan +
duration_bare_seconds переехал в тело»), зеркало реестра 1.1.22 → **1.1.26**.

### wireguard переехал на секцию реестра

Секции `mappers.uri` и `mappers.conf` приехали реестром ИСПОЛНЯЕМЫМИ и стали
основой. Наши полные черновики заменены **оверлеями** (`_overlay: true`) с
`_why` у каждого ключа.

Грабля, которую этот переезд вскрыл: черновик БЕЗ `_overlay` замещает
реестровую секцию ЦЕЛИКОМ. Секция реестра объявляет `emit: null` (обратного
хода у лаунчера нет вовсе — share-URI собирает только LxBox), и после синка
`toUri()` у wireguard падал `StateError` на каждом узле. Полная копия секции
при исполняемой реестровой запрещена не из чистоплотности: она молча
проигрывает реестру и перестаёт исполняться.

| Секция | Отличие | Почему |
|---|---|---|
| `uri` | `emit` целиком наш | реестр несёт `emit: null` |
| `conf` | `ini_dialect` | секция реестра диалект INI НЕ объявляет: у Go правила чтения зашиты в ридер. Передано лаунчеру — диалект свойство ФОРМАТА, а не реализации; держится под `_awaitingContractSync` |
| `conf` | `label.fallback` — литерал `WireGuard` | реестр уводит фолбэк в шаблон `{scheme}-{server}-{server_port}`. Цепочка ЗВЕНЬЕВ при этом реестровая буква в букву, включая `hint` (у нас он живой — `nameHint`) и хост `peers[].address` |
| `conf` | `unknown_key: drop` | тело собирается для ЯДРА: незнакомый ключ роняет запуск целиком, а `.conf` человек приносит руками из чужой панели |
| `conf` | `defaults.peers[].port` | `.conf` законно несёт `Endpoint` без порта; у лаунчера такой элемент отбраковывается |

Снято: оверлей `include` у `uri/trojan` и `uri/vless` — контракт 1.1.26 сам
подключил им `dialer#uri` (D133-26), дельта закрыта В НАШУ СТОРОНУ.

### Дефекты, найденные переездом

- **`format: base64_32` и `normalize: base64_std` судили ключ СТРОГИМ
  декодером.** Ядро (Go `encoding/base64`) принимает неканоническую последнюю
  группу: `…ccC=` и `…ccA=` — одни и те же 32 байта (D-030). Законный ключ
  падал с `wg_key_invalid`, и корпус `uri_psk_keepalive` (ключи в query)
  переставал разбираться ЦЕЛИКОМ. Оба выражения переведены на ленивый декодер,
  которым ключ судил парсер ссылки; у декодера появилось публичное имя.
- **Границы `min`/`max` у `int_array` не проверялись вовсе** — ветка списка
  смотрела только `len` и возвращалась. `reserved=1,2,999` уезжал в ядро.
  Границы относятся к ЭЛЕМЕНТУ: длину задаёт `len`, второго смысла у них нет.
- **Служебный `_why` ВНУТРИ `defaults`** доезжал до узла полем (`unknown_key`
  с путём `_why`): у этого ключа каждый ключ — путь тела. Причина выносится
  отдельным ключом секции (`_why_defaults`).

### Снято с нашей стороны

- умолчание **«одинокий userinfo → первое имя `into`»**: контракт 1.1.25
  проставил `single_into` всем 14 секциям с `userinfo`, включая те, где оно
  совпадает с `into[0]`, и завёл линтер. Догадка стирала разницу между
  «совпало» и «никто не смотрел»;
- `$code_pending` у **`wgconf_dns_ignored`**: корпус этой версии кода ЖДЁТ
  (`wgconf/ini_basic.expected.json` несёт его единственным warning).
  `wgconf_extra_peer_dropped` остаётся отложенным — корпусного кейса у него
  по-прежнему нет вовсе.

### Эмит AWG

Написание схемы возвращено к сегодняшнему виду: узел С awg-полями уезжает
`wireguard://…?jc=…`, как ждёт `emit_before480.json` (все 65 кейсов, включая
24 awg-именованных). Исключение вида ссылки из стража снято.

Написание `awg://` остаётся за узлом «род awg, ни одного awg-поля в теле», и
выразить его сегодня НЕЧЕМ: рода в каноническом теле нет, а вход эмита —
тело. Обе ветки `any_set` дают одно написание намеренно, чтобы перечень
awg-путей остался на виду.

### Тесты, которые шли БЕЗ реестра

`amnezia_link_test`, `wireguard_edge_test` грузили только черновики. Пока
черновик был полной копией секции, это работало; с оверлеем накладывать
стало не на что, и продакшен-путь `vpn://` → wireguard не давал НИ ОДНОГО
узла. Добавлена загрузка реестра рядом с черновиками.

Golden-судьи хранения: `*.config.json` совпадают БАЙТ В БАЙТ — тела и
identity сохранённых узлов не сдвинулись. Разошёлся только список
предупреждений, где прибавилась одна строка про DNS из `.conf`.

## 11. Что вышло: синк 1.1.28, корпус

Источник — `develop` лаунчера `b57625ea` («снят рукописный конвертер Xray»),
зеркало реестра 1.1.26 → **1.1.28**. Главное этого синка не в новых полях, а
в том, что корпус контракта впервые прогнан ЦЕЛИКОМ: в чистом worktree
`app/contract/` нет (каталог gitignored), и 130 тестов корпуса молча
скипались. После синка их 1165, и красное в них — не регрессия синка, а
то, чего раньше никто не смотрел.

### Носитель наложенного слоя не считался объявленным

Настоящая регрессия движка, найденная корпусом. `overlays[]` называет
параметр-НОСИТЕЛЬ своим `source` (`query.extra`), а записи читают слой под
его именем (`extra.mode`). Набор «объявленных» собирался только из `source`
записей, и плоского `query.extra` там не было ни у кого: носитель уезжал в
`uri_param_unknown`. Шесть кейсов `vless/xhttp_extra_*`, включая тот, где
слой битый и записей не даёт вовсе — там код был единственным содержимым
конверта.

### Q133-54: зеркальные записи оверлеев

Норма MAPPER_ENGINE §7.1: запись БЛОКА изымается из плана только когда
одноимённая запись секции несёт ТОТ ЖЕ полный набор `source`. Наши оверлеи
держали копии реестровых записей, и этому условию они не отвечали —
вставали РЯДОМ с реестровой и решали спор за путь своим `priority`.

| Запись | Было | Чем кончилось |
|---|---|---|
| `ws.ed` | `priority: -1` против реестровых 5 | плоская пара `?ed=` била хвост пути |
| `ws.eh` | `priority: 0` против 30 | то же |
| `xhttp.path` | без `extract` | терялось срезание `?`-хвоста (SPEC 002 §4.1) |
| `$selector`, `httpupgrade`, `grpc`, весь `xhttp` вида xray | своё написание `source` | состав полей выходил смешанный — 16 кейсов `xhttp_test` |

Сняты все, кроме `ws.ed`: у неё есть НАСТОЯЩЕЕ отличие — реестр подставляет
`Sec-WebSocket-Protocol` через `implies`, мы нет (D-008, зафиксировано парой
override'ов корпуса). Запись приведена к реестровым `priority` и набору
`source`, то есть теперь переопределяет по норме, а не спорит.

### Снято с нашей стороны

- `xray/hysteria2` черновик ЦЕЛИКОМ: контракт привёз ДВЕ секции со
  взаимоисключающими `detect` по версии (TASKS_LXBOX §26), обе исполняемы.
  Наш черновик ронял версию 1 кодом `protocol_unsupported`, у реестра она
  становится живым узлом рода `hysteria` — дельта односторонняя, принята
  лаунчером. Полная копия при исполняемой секции реестра молча ей
  проигрывает и перестаёт исполняться (та же грабля, что у wireguard в
  1.1.26).
- ожидание «hysteria2 из Xray не получает `tls_not_applicable_quic`»:
  правило реестра (`tls.json`, `body.fields.utls.forbidden_for`) снимает
  блок САНИТАЙЗЕРОМ и одинаково на трёх входах. Прежняя рукописная ветка
  молчала не по правилу, а потому что эмитить отпечаток было некому.

### Дефект, найденный переездом — ЗАКРЫТ (написание по форме)

Обратный ход выводил ключ контейнера v2rayN из таблицы и брал ПЕРВЫЙ
источник записи. У записи с ФОЛБЭКОМ источников канон и написание
контейнера расходятся: `grpc.serviceName` читает `serviceName` ∥
`service_name` ∥ `path`, каноном зовётся по первому, а контейнер знает
только `path`. Пока ключ `path` держала наша зеркальная запись `http.path`,
это не стреляло; с её снятием grpc-узел уезжал в ссылку ключом, которого
чужой клиент не читает, и терял `service_name` на круге
(`vmess_pipeline_invariants`, кейс «VMess gRPC»).

Оверлеем-перестановкой это не лечится: перестановка цепочки в нашей записи
набор `source` меняет, и по §7.1 реестровая запись не изымается — обе
остаются в плане. **Норма принята лаунчером 19.09.2026: написание выбирает
ФОРМА, и `emit.names` сильнее «первого источника», в том числе у
формы-контейнера; допустим и `forms[].emit.names`.** Движок читает оба
места, конкретное (форма) бьёт общее (секция); написание по-прежнему
ограничено набором, который запись ЧИТАЕТ, иначе своя ссылка обратно не
разберётся. Написание объявлено оверлеем `uri/vmess.json`
(`emit.names: {serviceName: path}`), где форма у эмита одна.

Переезд ключа обнажил три смежных умолчания обратного хода в контейнер,
починенных тем же заходом:

| Что | Было | Стало |
|---|---|---|
| `when` записи | не проверялся вовсе — ключ `path` доставался ПЕРВОЙ по обходу записи (безусловной `http.path`) | ключ не занимает запись, чьё условие тело ОПРОВЕРГАЕТ; `path` читают ws, http, httpupgrade, xhttp и grpc, тело подтверждает одну |
| `compose` и одногрупповой `extract` | в контейнер не уезжали вовсе: у таких записей нет `maps_to`, и в карту ключей они не попадали | уезжают; у ws путь лежит ровно в такой записи, и вместе с ним доезжает хвост `?ed=` |
| текст значения | `'$v'` — список уезжал дословным `[cdn.example.com]`, и круг читал имя хоста со скобками | `_serializeValue` записи, то же правило, что и в query |

Три строки разрешённых отличий `engine_emit_shape_test` сняты: у `grpc_tls`,
`json_net_h2_http_transport`, `json_net_xhttp` и `xhttp_fallback_httpupgrade`
эмит совпал со снимком рукописного БАЙТ В БАЙТ.

### naive+quic: потеря починена, а не норма сменилась

Рукописный `toUriNaive` ВСЕГДА писал `naive+https://` — ветки QUIC у него не
было вовсе, и узел `naive+quic://` на круге ронял `quic` и
`quic_congestion_control`: по Copy link уезжала ссылка на ДРУГОЙ транспорт.
Тест держал это как «исключение круга».

Реестр написание объявляет сам —
`emit.form_from: {quic: {true: "naive+quic", "*": "naive+https"}}`, зеркально
`scheme_sets`. Движок исполняет объявленное, круг сходится полностью, и узел
с QUIC больше не исключается из корпусного круга. Страж вида ссылки на кейс
`quic_userpass` не срабатывает: снимок `emit_before480` держит там СТАРУЮ
ссылку, в которой QUIC уже потерян, и страж кормится ею.

### Красное, что осталось корпусу

`direction_corpus` чинился не секцией, а загрузкой: раннер строит узлы кейса
`parseUri`-ссылкой, а схема на движке без реестра не разбирается вовсе — 19
кейсов падали пустым составом группы, то есть жаловались на модель
Направления, к которой отношения не имели.

Остальное — РАСХОЖДЕНИЯ СТОРОН, поведение по ним не менялось. Общая их
черта: per-app override корпуса завести с нашей стороны НЕЛЬЗЯ, потому что
`<case>.expected.lxbox.json` лежит в корпусе ЛАУНЧЕРА и приезжает синком, а
`app/contract/` у нас gitignored. Заводить их обязана вторая сторона, и
список передаётся ей:

| Кейс | Расхождение |
|---|---|
| `naive/unknown_query_ignored` | у нас `uri_param_unknown` на КАЖДЫЙ ключ с путём, у лаунчера один код без пути — **норма наша**, лаунчер правит у себя |
| `trojan/echfq_not_read`, `vless/reality_tcp_no_flow` | тот же код на `echfq`/`spx`; у лаунчера `unknown_key` на JSON-входе не исполняется вовсе (§25 п. 6) — доводит у себя |
| `naive/empty_host_rejected` | `parse_error` против `emit_error`+`field_missing` — **норма наша** (пустой host → dropped), лаунчер правит у себя |
| `wireguard/amnezia_vpn_awg*` | порядок `warnings[]`: наш `wgconf_dns_ignored` несёт `path: dns`, чужой — без пути, и `body.order` разводит их по-разному — **норма наша** |
| `hysteria/*` (8) | схема объявлена расширением чужой стороны, каталог скипается целиком — скип законен |
| `xray/balancer_group` | род группы: у нас `urltest`, у лаунчера `group` |
| `xray/malformed_stream`, `xray/unsupported_protocol`, `xray/hysteria_v1_skipped`, `xray/vless_encryption_junk`, `xray/vless_default_port` | бухгалтерия `dropped[]`: состав, `ref` и текст причины отброшенных элементов. Сам дефолт порта снят отдельно (коммит `18998a8d`, арбитр — исходники Xray); красным остаётся именно форма отчёта об отбраковке |
| `contract_24_2_rules` ×3 | `hellorandom*`→`randomized` (реестр: префикс есть ТОЛЬКО у Go, «Dart не матчит и уводит в junk»), пустой пароль tuic (реестр: Go принимает с warning, `tuic://uuid@host` проходит), алиас `splithttp` (реестр: ветку читает ТОЛЬКО Go, `у Dart в _xrayTransportFromStream ветки splithttp нет`) |

Все три кейса `contract_24_2_rules` реестр называет расхождением сторон
ПРОЗОЙ в собственном `impl`, то есть это не регрессия 480 и не наша норма:
тест гейтится наличием `contract/registry` и до появления корпуса скипался
целиком. Подгонять их под чужое поведение — менять `identity`, поэтому они
оставлены красными.

### Починено с нашей стороны этим заходом

| Кейс | Причина |
|---|---|
| `wireguard/reserved_*` (2) | CANON §6: границы `int_array` судятся ПОЭЛЕМЕНТНО, а в `value` кода уезжает ВСЁ ПОЛЕ (`[1 2 999]`). Мы показывали вырванный элемент |
| `xray/ss_basic`, `xray/ss_method_legacy` | у секции `shadowsocks#xray` нет перечня `unknown_key.ignore` (`protocol`, `tag`, `remarks`, контейнеры), хотя у vless/vmess/trojan он есть и зовётся «общим, не нашим». Заведён оверлеем `xray/shadowsocks.json` до появления у реестра |
| `folders_roundtrip`, `merge_sources_by_url` | `backup_corpus_test` не грузил секции движка: член папки строится из своей ссылки, и без реестра состав папки выходил списком ПУСТЫХ имён. Та же грабля, что у `direction_corpus` |

## 11a. Дефекты входа с эмулятора (19.09.2026)

Проверка на эмуляторе через Debug API `addFromInput` — тот же путь, что
вставка из буфера. Четыре дефекта; по каждому сперва воспроизведение
юнит-тестом на голове `develop`
(`app/test/parser/emu_input_defects_test.dart`).

| # | Дефект | Причина | Чем чинится |
|---|---|---|---|
| Д-3 | Xray-JSON не принимается ни одной из трёх форм (`400 No valid outbounds in JSON`) | на Xray в `source_kinds.json` была ОДНА ветка — `xray_config_array`. Одиночный `{"protocol":…}` не подходил никуда и доезжал до `uri_lines`; полный конфиг забирала `singbox_config` (50), у которой признак `outbounds` БЕЗ проверки диалекта, и элементы уезжали в sing-box-маппер | данными: ветки `xray_outbound_array` (33), `xray_outbound` (34), `xray_config` (35). Признак Xray — `protocol`, как `type` у sing-box; неоднозначный вход остаётся за sing-box, как и у пары 30/31 |
| Д-4 | `base64("vless://…\ntrojan://…")` отвергается вставкой | НЕ регрессия волны: гейт `_addJsonNodes` был таким же на `v2.24.4`. `decode` разворачивает оболочку и отдаёт `UriLines` (ветка `base64_wrapped`), а в контроллер приходил только `JsonConfig` | маршрут для уже готовой формы: оболочка снимается по признаку исходного текста (`://` в нём нет), контейнер — тем же порогом, что у JSON (§368/§129) |
| Д-2 | `mport` с host-частью роняет ВЕСЬ конфиг (`bad port range`) | `port_range_spec` (`interpreter.dart`) возвращал элемент с `:` КАК ЕСТЬ, без проверки, что по обе стороны числа. Восстановленная authority (`host:443`) проходила насквозь, мусор `abc` становился `abc:abc` | примитив движка: элемент, не являющийся парой чисел, выбрасывается. Имени схемы нет — проверяется форма элемента списка портов, как у соседнего `cidr_prefix` |
| Д-6 | REALITY `pbk` в std-base64 проходит разбор, ядро падает (`illegal base64 data`) и роняет ВЕСЬ конфиг | годность судилась верно (`isValidRealityPublicKey`: оба алфавита, 32 байта — как объявляет реестр), а НАПИСАНИЕ не переводилось. Ядро декодирует `public_key` только `RawURLEncoding` | `normalizeRealityPublicKey` рядом с гейтом: значение, декодируемое в 32 байта, приводится к RawURL. Негодное возвращается как пришло — судит его гейт блока. Прецедент — `normalizeAwgHeaderKey` (§481) |

**Дельта — одна, Д-6.** Ключ REALITY, написанный std-алфавитом, меняет тело
и `identity`: было `"Awo+GB8m…"`, стало `"Awo-GB8m…"` — тот же ключ
побайтно, другое написание. Кейсы `b480:pbk_encoded_plus` и
`b480:pbk_raw_plus` (`vless`). Пометка двусторонняя: `_delta480` +
`_before480` у кейса в `pipeline_identity_before.json` и
`emit_before480.json`, плюс поимённая строка в `allowed`
у `engine_emit_shape_test`. Узел, чей ключ УЖЕ в RawURL, не двигается —
тела рабочих узлов прежние.

Д-2 и Д-3 дельтой не являются: до них этот вход не давал узла вовсе (Д-3)
либо давал конфиг, который ядро не поднимало (Д-2).

### Запросы лаунчеру

| Что | Почему |
|---|---|
| `body.fields.server_ports` (`hysteria2.json`): формат ЭЛЕМЕНТА списка | тип `listable_string` без формата — санитайзеру нечем проверить, что элемент вида `N:M`. Пропуск стоит ВСЕГО конфига (`bad port range` — фатал ядра), поэтому сегодня отбор живёт на маппере. Тот же разрыв у `alpn` (`tls.json`), где drop элемента тоже рукописный |
| `tls.json` → `reality.public_key`: `normalize` рядом с `format: base64_32` | реестр объявляет законными ОБА алфавита, а ядро декодирует только RawURL. Сегодня правило выражается лишь кодом (`normalizeRealityPublicKey`); симметричный приём просится в реестр — как `base64_std` у санитайзера, но в другую сторону |
| `source_kinds.json` у лаунчера | три ветки Xray заведены черновиком с нашей стороны (`_why` у каждой). Форма файла принята лаунчером за основу (TASKS_LXBOX §24.27), но самих веток у него нет |

## 12. Что вышло: синк 1.1.33 → 1.1.35

Источник — `develop` лаунчера `c85574ef`, зеркало реестра 1.1.28 → **1.1.35**
(три контракта одним заходом: 1.1.33 приехал первым, 1.1.34 и 1.1.35 догнали
по ходу работы). Тема синка одна: то, что у нас жило кодом, лаунчер объявил
данными, и наши оверлеи под это стали лишними.

### Диалект INI — из реестра (1.1.30, §28)

`mappers.conf` получил блок `ini_dialect`: регистр ключей и значений,
префиксы строчных комментариев, судьба повторного ключа, судьба повторной
секции. Наш одноимённый оверлей снят целиком, вместе с ним ушли
`$code_pending` и `_awaitingContractSync`. Правило второй `[Peer]` теперь
атрибут данных (`repeat: first_only` + `on_extra.code`), а не выбор кода:
первая становится узлом, повторы отброшены с `wgconf_extra_peer_dropped`.

Черновика `conf/` не осталось вовсе — последнее отличие, дефолтный порт пира,
сняла ветка `endpoint.on_no_match.defaults` реестра. Оверлей был не просто
лишним, а вредным: он заполнял `peers[].port` безусловно, и из-за этого
проверка обязательности `Endpoint` не срабатывала даже после починки.

### Три дефекта входа `.conf` (1.1.31, §29)

Лаунчер нашёл их у себя на переводе боевого пути и попросил проверить у нас.
Проверено фактом, тесты — `test/parser/engine_conf_defects_test.dart` (16
кейсов, включая контрольные «как было»).

| Дефект | У нас |
|---|---|
| `required` не проверялся у записи с `extract` | **ВОСПРОИЗВЁЛСЯ.** `paths` собирались из `maps_to`/`split_into`, у записи `endpoint` ни того ни другого нет — проверка уходила в `continue`. `[Peer]` без `Endpoint` становился узлом с пиром без адреса и порта. Починено: «обязательна» = «заполнен хоть один из объявленных путей», считая `extract.into` и адресата `on_no_match: take_all` |
| `+` → пробел в ini-пространстве | **не воспроизводится.** Источник `ini.*` возвращает значение сырым: ни percent-декода, ни форм-семантики на этом пути нет вовсе. Закреплено тестом на ключе с `+` |
| `hint` в цепочке метки не исполнялся | **не воспроизводится.** `hint` читается наравне с прочими источниками, корпус его и ждал. Закреплено тремя тестами: комментарий сильнее `hint`, `hint` сильнее фолбэка, фолбэк последний |

### Незнакомый ключ `.conf` перестал теряться молча (1.1.32)

Тот же класс, что и у лаунчера (его Q133-58), и ровно тот же корень: проверка
необъявленного спрашивала `space.query` и `space.json`, а у документа предмет
другой — ключи секций ini. Запись `unknown_key` у секции стояла и раньше, но
исполнителя не имела. Добавлен `_reportUnknownIni` и набор `_declaredIni`;
объявленность считается **по источнику** (`ini.Peer.PersistentKeepalive`), а
не по имени записи, иначе объявленным не выглядел бы ни один ключ файла.
Секция в имени значима: `MTU` у `[Interface]` и `MTU` у `[Peer]` — разные
ключи. Игнор-список wg-quick сверяется и с полным `секция.ключ`, и с голым.

### Записи-`null` объявлены (1.1.34, §31.2)

Загрузчик общих блоков дропал запись `"spx": null` как «не Map», и параметр
переставал быть объявленным: `echfq` у trojan и `spx` у vless уезжали в
`uri_param_unknown`. Теперь `null` — это объявленное «знаем, читать нечего»:
имя названо, кода нет, в тело ничего не едет. Два кейса корпуса ссылок
позеленели.

### Имя в `query_name`, порядок кодов маппера

Тексты `uri_param_unknown` / `json_field_unknown` / `wgconf_param_unknown`
зовут `{query_name}`, а движок клал имя только в `path` — человек увидел бы
плейсхолдер буквально. Имя теперь едет дважды и намеренно: в `path` ради
дедупа по паре «код, путь», в `params.query_name` ради текста.

Порядок `warnings[]` расходился на `amnezia_vpn_awg*`: `wgconf_dns_ignored`
несёт `path: dns`, а `dns` — имя записи, не путь тела, и сортировка по
`body.order` уводила его в хвост, тогда как у лаунчера он первый. Раннер
корпуса теперь знает имена записей с `maps_to: null` и ставит такие коды
впереди тела: потеря на разборе случилась раньше, чем тело вообще собралось.

### Тексты AWG — из реестра (1.1.33)

`awg_header_invalid` и `awg3_field_invalid` держали текст в коде ровно
потому, что реестровый умел подставить одно `{field}` и давал «field {field}
removed». Контракт 1.1.33 переписал оба с `{path}`/`{value}` и последствием
для рукопожатия. Классы `AwgHeaderInvalidWarning` и `Awg3FieldInvalidWarning`
сняты, коды идут `RegistryWarning`; четыре ключа словаря (ru + zh) удалены,
все чекеры `tool/l10n` зелёные. Хранимая форма не менялась — `StoredWarning`
описан кодом и `params`, старые записи читаются как прежде.

### Оверлеи, снятые как совпавшие

- `conf/wireguard.json` — целиком (файл удалён, строка ассета из `pubspec`
  тоже: пустой каталог ломает сборку);
- `xray/shadowsocks.json` → остался только `password` с `empty: significant`;
  `unknown_key.ignore` приехал в реестр байт в байт (1.1.34);
- `xray/vless.json`, `xray/trojan.json` → `unknown_key` снят, тот же список;
- порт `ss` по умолчанию 8388 (D133-29) — **нашего оверлея не было**, снимать
  нечего.

### §30 (D133-6): миграция тегов

Читано, действий не требует. У лаунчера identity = тег, и он готовился
переписывать ссылки; проверка на 22 написаниях дала посимвольное совпадение,
миграция не понадобилась. У нас identity — не тег, источник хранится и
перепарсивается, так что и повода не было. Расхождение по `ss`/`socks*`
(`ss-host-port` против `shadowsocks-host-port`) остаётся в силе и снимается
только решением владельцев с обеих сторон.

### «Уровень документа» → «вид источника»

Решение владельца 19.09.2026. `contract_draft/documents.json` переименован в
`source_kinds.json` (загрузчик читает и реестровое `source_kinds.json`, и
прежнее `sources.json` — у лаунчера файла ещё нет, черновик остаётся нашим с
`_awaitingContractSync`). Термин заменён в комментариях движка, тестах и
здесь; ключ вида — `source_kind`, слово `source` без `kind` означает
подписку, а откуда запись берёт значение — `from`. Имена классов и файлов
тестов не трогались: они внутренние.

### Не входит в этот заход

- Диалекты sing-box-входа (форки) — решение владельца, отдельной задачей.

### Осталось красным

Единственный поимённый список красных кейсов корпуса по фиче. Сверен
прогоном 24.09.2026 (§513): контракт 1.1.46 (`app/contract` восстановлен из
lock, `7e2945bf`), `develop` `5150ac74`; гонялись только перечисленные здесь
кейсы и оба tuic, а не корпус целиком. Итог — **шесть** красных:
`naive/empty_host_rejected` и пять `xray/*`. Прежние счёты «`xray/*` (6)»
(прежняя редакция этой таблицы) и «9 красных» (§14) устарели:
`xray/vless_default_port` позеленел, оба tuic (`password_empty_after_colon`, `password_absent_no_colon`) зелёные.
На CI корпус скипается (`app/contract/` в gitignore), так что зелёный CI
этих кейсов не видит.

| Кейс | Почему |
|---|---|
| `naive/empty_host_rejected` (uri) | `parse_error` против `emit_error`+`field_missing`. Норма наша, лаунчер правит у себя |
| `xray/balancer_group` (body) | род группы: у нас `urltest`, у лаунчера `group` |
| `xray/malformed_stream` (body) | бухгалтерия `dropped[]`: отброшенный элемент не попадает в отчёт |
| `xray/unsupported_protocol` (body) | бухгалтерия `dropped[]`: то же |
| `xray/hysteria_v1_skipped` (body) | состав узлов, а не `dropped[]`: ожидание корпуса 1.1.46 несёт узел hysteria v1 (`hy.example:443`, `auth_str`), у нас узла нет вовсе — вопреки имени кейса лаунчер его больше не пропускает |
| `xray/vless_encryption_junk` (body) | бухгалтерия `dropped[]`: код верный (`vless_encryption_invalid`), `ref` наш — тег outbound'а (`proxy`), у лаунчера — имя элемента (`enc-junk`) |
| `contract_24_2_rules`: пустой пароль tuic | **закрыто у нас, открыто у лаунчера.** Владелец решил дельтой `delta480-7`: узел остаётся, пустой пароль назван кодом `password_empty`. Тест переписан под это. Лаунчер держит случай открытым (Q133-67): данными не выражается, кейса корпуса нет — расхождение до его ответа |

### delta480-8 — `splithttp` в ссылке читается как `xhttp`

Регрессия перевода ссылки на движок, найдена после синка 1.1.35. `splithttp`
— прежнее имя `xhttp`, и у входа Xray реестр алиас несёт сам
(`blocks.xray.$selector.network.value_map`). У ссылки он не выражен: набор
`blocks.uri.$selector.type` закрыт (`when.in`), `splithttp` в нём не
значится, а `when.in` подавляет запись целиком — селектор не срабатывает, и
транспорта в теле не появляется вовсе. Рабочий узел уезжал в конфиг голым
TCP на порт, который ждёт HTTP, и молча: кода узел не получал.

Рукописный маппер алиас знал (`transport.dart`, `case 'splithttp':` рядом с
`case 'xhttp':`), потеря пришла с переводом vless/vmess/trojan на движок.
Корпус ссылок кейса не несёт — нормирован только вход Xray
(`body/xray/vless_splithttp`), поэтому регрессию не поймал ни один судья.

Закрыто **данными**: запись `$selector.type` в оверлее
`contract_draft/uri/transports.json`. Запись скопирована из реестра целиком
(оверлей замещает запись, а не сливает её поля), отличий ровно два —
`splithttp` в наборе `when.in` и пара `splithttp → xhttp` в `value_map`.
Тело и identity совпадают со ссылкой, написанной каноническим `xhttp`;
держит это тест «splithttp и xhttp дают одно тело и одну identity».

Расхождение передано лаунчеру и **закрыто им в контракте 1.1.37**:
`splithttp` заведён в реестровый набор `blocks.uri.$selector.type`. Оверлей
снимает агент синка 1.1.37 — после него запись `$selector.type` из
`contract_draft/uri/transports.json` уходит целиком, поведение и тела узлов
остаются прежними (реестровая запись говорит ровно то же). Пометка
`_awaitingContractSync` стоит у записи и служит адресом этой уборки.

Строки таблицы, кроме пустого пароля tuic, красными больше не стоят: зелёных
`contract_24_2_rules` 17 при одном законном скипе.

Ожидание `hellorandom*` исправлено у нас: тест требовал `random` для всего
префикса, повторяя прозу лаунчера времён до 1.1.28. Данные говорят иначе —
`hellorandomized` → `randomized`, и движок матчит длинный префикс раньше
короткого, то есть был прав всё это время.

## 13. Что вышло: синк 1.1.37

Источник — лаунчер `1d32af49` (контракт **1.1.37**, поверх 1.1.36, где эмит
переехал на движок и атрибуты обратного хода забраны в реестр).

**Два довеска контракта закрыли два наших ожидания.**

| Ожидание | Чем закрыто |
|---|---|
| `password_empty` у tuic стоял БЕЗ текстов: код ставил наш оверлей, а в `warnings.json` его не было | Реестр привёз код ОБЩИМ (не под схему) с текстами en/ru, причиной и шагами починки. Примитив тот же — `on_empty`, имя кода то же. Оверлей `uri/tuic.json` снят ЦЕЛИКОМ: реестр несёт и `emit.names` этой схемы, принятый у нас как есть |
| `type=splithttp` в ССЫЛКЕ терял транспорт молча — узел уезжал голым TCP | Реестр добавил `splithttp` и в набор `when.in`, и в `value_map` записи `blocks.uri.$selector.type`. Наш оверлей снят: реестровая запись вдобавок объявляет `emit_as: raw` — обращение `value_map` неинъективно, и на выходе обязан писаться канон `xhttp`, а не алиас |

**Оверлеи эмита, снятые за реестром:** `uri/hysteria2.json` целиком (блок
`emit` совпал дословно); `transports` → `xhttp.noGRPCHeader`,
`noSSEHeader`, `xPaddingObfsMode` — были сняты как совпавшие, но ВЕРНУЛИСЬ
(см. ниже).

**Оверлеи, которые остались, и почему.** Каждый — расхождение сторон, а не
наш долг:

- `uri/shadowsocks.json` (НОВЫЙ) — `emit.userinfo.padding: false`. Реестр
  объявил паддинг base64 истиной и сам назвал расхождение: «Go пишет “=”,
  Dart срезает, обе стороны читают обе формы». Забрать реестровое значило бы
  сменить вид КАЖДОЙ ss-ссылки (`…cGFzczEyMw@host` → `…cGFzczEyMw==@host`),
  то есть и сохранённый `rawSource` ручных узлов, и то, что уезжает по Copy
  link;
- `transports` → три записи xhttp с `emit_as: raw`. Реестр это объявление
  снял, потому что У ЛАУНЧЕРА оно избыточно: его умолчание для любого
  необъявленного булева — СЛОВО `true`, а цифру даёт только явный
  `emit_as: bool01`. У нас умолчание ОБРАТНОЕ. По типу (`bool_spelled`) это
  решать нельзя — проверено: `reduce_rtt` у tuic объявлен тем же типом, и
  привязка сменила бы вид ссылок tuic (`reduce_rtt=1` → `=true`);
- `transports` → `ws.ed` сохраняет ОТСУТСТВИЕ реестрового
  `round_trip: false`. У лаунчера хвост пути пишется всегда, потому что его
  разбор дописывает `early_data_header_name` конвенцией; у нас (отступление
  D-008) не дописывает, хвост пишется только при подтверждённом теле, и
  отказ от обратного хода ронял бы `max_early_data` молча;
- `uri/vmess.json` → `json_map: null`. Реестр объявил карту контейнера
  ссылками трёх новых видов (`=…` литерал, `$label` метка,
  `$param.<имя>` значение одноимённой записи). Наш движок карту не
  объявляет, а ВЫВОДИТ из таблицы, и объявленная карта вывод закорачивает:
  ссылки vmess теряли `net`/`host`/`path`/`tls` — узел уезжал голым tcp без
  TLS. Поддержка трёх ссылок — работа отдельная.

**Движок.** Объектная форма `compose` (примитив 1.1.36) роняла загрузку
секции целиком: `MapperParam.fromJson` кастовал `compose` к `String?`
слепо. Починено; заодно объектный `compose` принимает `from` СПИСКОМ путей —
форма реестра, в которой шаблон адресует пути напрямую. `omit_when_empty`
приведён к семантике лаунчера: он про ПУСТОЕ значение, а непустую группу не
срезает (пустую шаблон срезает и без списка).

**Корпус.** `test/contract/body_contract_test.dart` на develop до синка нёс
10 красных; после синка их 6 — синк чинит четыре и не ломает ни одного.
Оставшиеся — долг, к этому синку отношения не имеющий.

**Не делалось** (снято по бюджету): сверка находок эмита лаунчера
Q133-70…75, новые примитивы `round_trip_only: "emit"` и `emit.refuse_when`,
сравнение списка разрешённых отличий с D133-E1…E9.

## 14. Что вышло: синк 1.1.42

Источник — лаунчер `dfed276c` (контракт **1.1.42**, поверх 1.1.37).
Предрелизный синк v2.25.0: оба приложения на одной версии реестра.

**Три наших запроса приняты реестром — код снят, правило стало данными.**

| Было кодом у нас | Стало атрибутом реестра |
|---|---|
| `port_range_spec` (маппер, 44f26d11) выбрасывал негодный элемент `server_ports` МОЛЧА и только на входе ссылки | `item_pattern` + `on_item_invalid` у поля тела (1.1.40): элемент выбрасывается С КОДОМ и путём-элементом, на ВСЕХ входах. `port_range_spec` остался формой записи (дефис → двоеточие, одиночный порт → пара), суд отдан реестру — `delta480-9` |
| `normalizeRealityPublicKey` (`uri_utils.dart`, 9b072ca7) приводил ключ REALITY к RawURL только на JSON-входе | `normalize: "base64_rawurl"` у `tls.reality.public_key` (1.1.40). Рукописный перевод снят целиком: оба снимка-дельты (`b480:pbk_encoded_plus`, `b480:pbk_raw_plus`) дают прежнее тело |
| черновик `contract_draft/source_kinds.json` — наша форма файла видов источника | `registry/source_kinds.json` (1.1.41). Черновик снят: состав веток совпал ДО ЗАПИСИ, включая три ветки Xray нашей находки Д-3 |

**Тот же разрыв у `tls.alpn`** закрыт реестром заодно: `item_pattern` плюс
`normalize: "trim"` рядом (`alpn=h2, h3` даёт элемент с ведущим пробелом, и
без обрезки формат выбросил бы живое значение). Порядок нормативен:
`normalize` → элементы → ограничения поля.

**Длина 32 у `base64_rawurl` — часть условия, а не суждение.** `enabled` —
законный base64 на 5 байт, и перекодируй его нормализация, человек увидел бы
в коде `reality_pbk_invalid` значение `enablec` вместо написанного им
`enabled`. Канонизируется только то, что ядро вообще возьмёт ключом.

**Имена видов источника читает загрузчик.** Реестр зовёт записи
`source_kinds.kinds` и `source_kind`, черновик звал `sources` и `kind`
(у лаунчера `kind` занято видом источника СЕКЦИИ — `mappers.<kind>`).
`max_unwrap_depth` переехал внутрь узла. Обе формы читаются, чтобы синк не
требовал переписывать уже написанное.

`source_kinds.json` грузится отдельным списком (`_kStandaloneShared`): цели
`ref` у него нет — тело узла в него не спускается, его читает опознание
ИСТОЧНИКА. Отсутствие файла осталось рабочим состоянием: контракт старше
1.1.41 его не несёт.

**Забрано без правок кода:** тексты кодов en/ru (1.1.38); `on_len_gt` у
`$extra_*` (1.1.42) — примитив уже исполнялся, кейс `body/xray/vless_extra_vnext`
зелёный; порядок `warnings[]` по CANON §6 (коды записей маппера с
`maps_to:null` впереди кодов тела) — у нас уже так; `value` у
`wgconf_dns_ignored` — приехал ожиданиями корпуса. Список
`_awaitingLauncherGendocs` в `docs_mirror_test` пуст: якорь `password_empty`
приехал зеркалом.

**Корпус.** 9 красных (счёт на момент синка; сверка §513 от 24.09.2026 даёт
шесть — поимённо в §12 «Осталось красным»), все — долг, к этому синку
отношения не имеющий и записанный в разделе 12: шесть `xray/*` (бухгалтерия `dropped[]` и род
группы), `naive/empty_host_rejected` (норма наша, лаунчер правит у себя) и
два tuic (`delta480-7`, у лаунчера Q133-67). Синк не сломал ни одного и
починил `hysteria2/mport_host_port_item_dropped` и
`vless/reality_pbk_base64_std`.

**Не делалось, и почему это не долг синка.**

18 кейсов лаунчера «текст → вид → число элементов» живут его Go-тестом
(`TestSourceKinds`), в общий корпус не вынесены — сверять у нас нечего.
Расхождений по составу веток при этом нет: наш черновик и реестр совпали
запись в запись.

`emit.refuse_when` (`wireguard`, `peers` длиннее 1) и `round_trip_only:
"emit"` (`dialer` → `detour`) реестр 1.1.42 объявляет, а движок их не
исполняет. Ни секция не перестаёт грузиться, ни линтер не краснеет, ни один
кейс корпуса не красный — примитивы у нас сегодня НЕДОСТИЖИМЫ: INI-разбор
сводит `.conf` с несколькими `[Peer]` к одному узлу с одним пиром ДО эмита,
так что отказывать эмиту не на чем. Заводить отказ, который никогда не
сработает, значило бы писать мёртвый код; понадобится он ровно тогда, когда
многопировый узел научится доезжать до эмита целым.

## 15. Что вышло: синк 1.1.46

Источник — лаунчер `7e2945bf` (контракт **1.1.46**, поверх 1.1.42). Четыре
минорных версии одним заходом: нормы §39–§41 и сводка решений §42.

**Корпус 1.1.43–1.1.46.** Кейсы merge-append / двойной base64 закрыты
§492, `dialerProxy` → freedom fragment — §488. Под нормы 1.1.43–44
движок общий, без имён схем: `on_empty` срабатывает и на `""`, пустой
хвост userinfo не пишется; `$ref` именованной таблицы после снятия
оверлея берётся из реестра, если в черновике блока её нет. Конвейер
кладёт `type_invalid` в `dropped[]` (D-088 / CIDR wg). Пустой пароль
tuic не материализуется в теле (§42.6).

| Версия | Тема | У нас |
|---|---|---|
| 1.1.43 | `merge: append`, двойная base64, BOM, порядок `value_map` | §492 |
| 1.1.44 | CIDR wg, emit multi-peer, дробные порты | уже чисто; кейсы корпуса |
| 1.1.45–46 | `dialerProxy` freedom+fragment → `tls.fragment`, без кода | §488 |

**Оверлеи класса A сняты (25 записей).** Совпавшие с реестром 1.1.46 ключи
emit у http/naive/anytls/trojan/vless/vmess/wireguard, `tls#uri` `fp_dialect`/
`sni`/`sid`, xray `include` у trojan/vless, `defaults` у vmess (файл удалён).
Оставлены расхождения B/C: ss padding, xhttp `emit_as`, ws `eh`, vmess
`json_map`/`param_order`, vless `omit_default`, xray `forms` на identity.

**Сняты `_awaitingContractSync`** у xray/trojan, xray/vless, xray/socks —
записи приехали в реестр (dialer#xray, detect формы).

**Не делалось.** Снимки identity/golden/emit не переписывались. Красные
корпуса naive/empty_host и часть xray/* — долг соседних агентов, не этот
синк. Поимённый список — §12 «Осталось красным».

## 16. Осталось: долги без задачи-владельца (§513)

Долги, которые после релиза v2.25.0 жили только в отчёте ревью
[506-change-review.md](../../tasks/506-change-review.md) (раздел 5
«Подтверждённые долги»). Записаны здесь, чтобы у каждого был адрес в спеке
фичи. Отдельных задач не заводилось.

| Долг | Что нужно | Чья сторона |
|---|---|---|
| `dropped[]` + `index` и `code` | в записи отбраковки нет индекса элемента в теле подписки, по которому вторая сторона сверила бы состав. Та же бухгалтерия держит красными три `xray/*` из §12 (отбраковка не попадает в отчёт или `ref` расходится) | ждёт лаунчер (506, раздел 5) |
| Q133-61: голый IPv6 в `Endpoint` `.conf` | `2001:db8::1:51820` без скобок: регулярка записи не совпадает, и весь текст уходит адресом с портом по умолчанию 51820 (`wireguard.json` → `mappers.conf.params.endpoint`, `on_no_match: take_all`); такой узел не соединится. Обе стороны ведут себя одинаково, корпус фиксирует это как есть. Починка меняет тело и identity живых узлов — только решением владельца и синхронно с лаунчером (QUIRKS лаунчера Q133-61) | решение владельца, затем реестр (506, раздел 5) |
| `contract_draft/singbox/*` (13 файлов) → реестр | порождённые секции `_generated: true`, наш вклад; перенести в реестр лаунчера, чтобы оверлеи ушли в ноль | лаунчер + синк у нас (506, раздел 5; `contract_draft/singbox/README.md`) |
