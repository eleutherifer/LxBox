Patch after v2.25.2. Core v1.14.1-lx.10: Diagnostics on a naive server no longer closes the app, WireGuard/AmneziaWG servers addressed by name connect in a second instead of five, and XHTTP connections are not marked failed when you switch servers. The wait for start grows with the number of WireGuard endpoints, and a forced stop names its reason. Servers is one list of all kinds. The config editor menu no longer piles up. DNS preset Shield really uses Yandex over DoT.
