Patch after v2.25.3. Core v1.14.2-lx.1: fewer tunnel resets on network change, no GSO noise in the AWG log. WireGuard/AmneziaWG servers are built on first use; at most 5 stay built by default (Settings: Lazy tunnel build, Built tunnels limit; 0 = no limit). Ru internet segment preset routes Russian apps by package. Two columns of servers on wide screens, Appearance tab. Duplicate servers in one subscription are collapsed. Proxy mode no longer asks about another VPN.
