Patch after v2.25.1. Subscriptions no longer come back short: amneziawg:// links, a vpn:// wrapper around a plain WireGuard config, a single Xray config, hysteria2 bandwidth and obfuscation are read. A line the app cannot use names its reason. An expired panel's decoy banner is not a server; kcp/quic and TCP header obfuscation are refused honestly. Workspaces no longer overwrite each other's subscriptions. Checking all servers survives several naive nodes. Selection in the config editor holds.
